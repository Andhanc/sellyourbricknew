# 🚨 КРИТИЧЕСКИ ВАЖНО: Исправление ошибки "Error configuring network"

## Проблема:
Railway не может настроить сеть, потому что:
1. ❌ **`SERVER_PORT` не установлен** в Railway Variables
2. ⚠️ Railway не может определить health check

## ✅ Решение:

### 1. УСТАНОВИТЕ `SERVER_PORT=3000` В RAILWAY:

**Railway Dashboard → Ваш сервис → Settings → Variables → Add Variable:**
- **Name:** `SERVER_PORT`
- **Value:** `3000`
- **Нажмите "Add"**

### 2. Проверьте, что установлено:

**Обязательные переменные:**
- ✅ `SERVER_PORT=3000` ← **КРИТИЧЕСКИ ВАЖНО!**

**Автоматически устанавливается Railway (НЕ добавляйте вручную):**
- ✅ `PORT` - Railway установит автоматически (например, 8080)

### 3. После установки переменных:

1. Railway автоматически перезапустит деплой
2. В логах должно появиться:
   ```
   [Vite Config] SERVER_PORT: 3000
   📋 Переменные окружения:
      - SERVER_PORT: 3000 ✅
   ```

## 📊 Что происходит сейчас (из логов):

✅ **Vite запустился** на порту 8080 (правильно)
✅ **Сервер запустился** на порту 3000 (правильно)
❌ **Но SERVER_PORT не установлен** - это может вызывать проблемы с проксированием

## 🔍 Health Check:

Railway проверяет доступность сервиса через:
- Основной порт (8080) - где слушает Vite
- Vite проксирует `/health` → сервер на порту 3000

Health check endpoints:
- `http://localhost:8080/health` → Vite проксирует → сервер `/health`
- `http://localhost:8080/` → Vite проксирует → сервер `/`

## ✅ После исправления:

1. Установите `SERVER_PORT=3000` в Railway Variables
2. Дождитесь автоматического перезапуска
3. Проверьте логи - должно быть:
   ```
   [Vite Config] SERVER_PORT: 3000 ✅
   📋 Переменные окружения:
      - SERVER_PORT: 3000 ✅
   ```
4. Деплой должен пройти успешно

## 🎯 Итого:

**Единственное, что нужно сделать:**
→ Установить `SERVER_PORT=3000` в Railway Dashboard → Settings → Variables

Всё остальное уже настроено правильно!
