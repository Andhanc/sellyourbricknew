# 🔍 Проверка redirect_uri в запросе к Google

## Проблема
Google все еще перенаправляет сразу на localhost, минуя Clerk. Нужно проверить, какой `redirect_uri` используется в запросе к Google.

## ✅ ШАГ 1: Проверьте redirect_uri в Network tab

### Как проверить:

1. Откройте DevTools (F12) → вкладка **Network**
2. Очистите список запросов (кнопка с крестиком)
3. Нажмите "Войти через Google"
4. **СРАЗУ** найдите запрос к `accounts.google.com` (обычно первый или второй)
5. Откройте его → вкладка **Payload** или **Query String Parameters**
6. Найдите параметр `redirect_uri`
7. **Скопируйте значение `redirect_uri`** и сообщите мне

**Ожидаемое значение:**
```
https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback
```

**Если там:**
```
http://localhost:5173
```
или
```
http://localhost:5173/profile
```
или любой другой localhost - значит в Google Cloud Console все еще есть localhost!

## ✅ ШАГ 2: Проверьте ВСЕ OAuth Client IDs

1. Откройте: https://console.cloud.google.com/apis/credentials
2. **Проверьте КАЖДЫЙ OAuth 2.0 Client ID** в списке
3. Для каждого:
   - Откройте его
   - Проверьте раздел **"Authorized redirect URIs"**
   - **УДАЛИТЕ ВСЕ строки с localhost**
   - **ОСТАВЬТЕ ТОЛЬКО:** `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
   - Сохраните

## ✅ ШАГ 3: Проверьте Client ID в Clerk Dashboard

1. В Clerk Dashboard: **User & Authentication** → **Social Connections** → **Google**
2. Проверьте **Client ID** - он должен быть:
   ```
   1028494020168-034f525smlhane9lc4f041bbugokjt1i.apps.googleusercontent.com
   ```
3. Если Client ID другой - найдите этот Client ID в Google Cloud Console и исправьте его Redirect URIs

## ✅ ШАГ 4: Создайте новый OAuth Client ID (если ничего не помогает)

Если после всех проверок проблема остается:

1. В Google Cloud Console создайте **НОВЫЙ** OAuth 2.0 Client ID
2. **СРАЗУ** добавьте правильный Redirect URI: `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
3. **НЕ ДОБАВЛЯЙТЕ localhost!**
4. Скопируйте новый Client ID и Client Secret
5. Обновите их в Clerk Dashboard
6. Попробуйте войти снова

## 📊 Что сообщить

После проверки Network tab сообщите:

1. **Какой `redirect_uri` в запросе к Google?**
   - `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback` (правильно)
   - `http://localhost:5173` (неправильно - значит в Google есть localhost)

2. **Сколько OAuth Client IDs в Google Cloud Console?**
   - Если несколько - нужно проверить все

3. **Какой Client ID используется в Clerk Dashboard?**
   - Должен совпадать с тем, который вы проверяете в Google Cloud Console

