# 🔑 Где найти Service ID и Public Key в EmailJS

## ✅ У вас уже есть:
- **Template ID:** `template_yzcrytx`

## 📍 Где найти Service ID:

### Вариант 1: Из списка Email Services
1. В левом меню EmailJS нажмите **"Email Services"** (или **"Services"**)
2. Вы увидите список подключенных email сервисов (Gmail, Outlook и т.д.)
3. Нажмите на ваш сервис (тот, который вы подключили)
4. **Service ID** будет показан вверху страницы (например: `service_xxxxxxx`)
   - Или в настройках сервиса
   - Обычно начинается с `service_`

### Вариант 2: Из настроек шаблона
1. Откройте ваш шаблон "One-Time Password"
2. В правой панели в поле **"From Email"** или в настройках шаблона
3. Там может быть указан Service ID

## 📍 Где найти Public Key:

1. В левом меню EmailJS нажмите **"Account"** (или кликните на ваш профиль/аватар)
2. Перейдите в раздел **"General"** (или **"Account Settings"**)
3. Найдите раздел **"API Keys"** или **"Public Key"**
4. Скопируйте **Public Key** (длинная строка, например: `xxxxxxxxxxxxx`)
   - Обычно это единственный ключ в этом разделе
   - Может быть помечен как "Public Key" или "API Key"

## 📝 Итоговый файл .env.local:

После того, как найдете все три значения, создайте или обновите файл `.env.local` в корне проекта:

```env
# EmailJS настройки
VITE_EMAILJS_SERVICE_ID=service_xxxxxxx
VITE_EMAILJS_TEMPLATE_ID=template_yzcrytx
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here
```

**Замените:**
- `service_xxxxxxx` на ваш реальный Service ID
- `your_public_key_here` на ваш реальный Public Key
- `template_yzcrytx` уже правильный (ваш Template ID)

## 🔍 Визуальные подсказки:

### Service ID выглядит так:
- Начинается с `service_`
- Пример: `service_abc123` или `service_gmail123`

### Public Key выглядит так:
- Длинная строка из букв и цифр
- Пример: `abc123def456ghi789` или `user_xxxxxxxxxxxxx`
- Обычно 20-30 символов

## ⚠️ Важно:

- **НЕ путайте Public Key с Secret Key!**
- Public Key безопасен для использования на клиенте
- Secret Key (если увидите) - НЕ используйте его, это для сервера

## 🎯 Быстрая навигация в EmailJS:

1. **Service ID:** Меню → Email Services → Ваш сервис
2. **Template ID:** ✅ У вас уже есть: `template_yzcrytx`
3. **Public Key:** Меню → Account → General → API Keys

После настройки перезапустите сервер: `npm run dev`

