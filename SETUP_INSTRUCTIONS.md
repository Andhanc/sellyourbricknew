# 🚀 Быстрая настройка WhatsApp авторизации

## Шаг 1: Получите данные из Green API

В личном кабинете Green API найдите:

1. **idInstance**: `7105458006` ✅ (уже видно)
2. **apiTokenInstance**: 
   - Нажмите на иконку 👁️ (глаз) рядом с `apiTokenInstance`
   - Нажмите на иконку 📋 (копировать) чтобы скопировать токен
   - Сохраните его - он понадобится!

## Шаг 2: Подключите WhatsApp

**Важно!** Статус показывает "Неавторизован" - нужно подключить WhatsApp:

1. В панели Green API нажмите кнопку **"Связать по QR-коду"**
2. Откройте WhatsApp на телефоне
3. Перейдите: **Настройки → Связанные устройства → Связать устройство**
4. Отсканируйте QR-код с экрана
5. Дождитесь, пока статус изменится на **"Авторизован"** ✅

## Шаг 3: Создайте файл .env

1. В корне проекта создайте файл `.env` (без расширения!)
2. Скопируйте содержимое из `.env.example`
3. Замените значения:

```env
VITE_GREEN_API_URL=https://api.green-api.com
VITE_GREEN_API_ID=7105458006
VITE_GREEN_API_TOKEN=ваш_скопированный_токен_здесь
```

**Пример:**
```env
VITE_GREEN_API_URL=https://api.green-api.com
VITE_GREEN_API_ID=7105458006
VITE_GREEN_API_TOKEN=abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
```

## Шаг 4: Перезапустите сервер

1. Остановите текущий сервер (Ctrl+C в терминале)
2. Запустите снова: `npm run dev`
3. Откройте сайт: `http://localhost:5173`

## Шаг 5: Проверьте работу

1. Нажмите "Войти" или "Регистрация"
2. Нажмите "Войти через WhatsApp"
3. Введите ваш номер телефона (тот, который подключен к Green API)
4. Нажмите "Отправить код"
5. Проверьте WhatsApp - должен прийти код!
6. Введите код на сайте
7. Должна произойти авторизация ✅

## ⚠️ Важные моменты

- **Номер телефона** должен быть в формате: `79991234567` (без +, пробелов, скобок)
- **Токен** должен быть скопирован полностью (это длинная строка)
- **Статус** должен быть "Авторизован" перед использованием
- **Лимит**: Бесплатный тариф - 100 сообщений в день

## 🐛 Если что-то не работает

### Код не приходит:
- Проверьте, что статус "Авторизован" в Green API
- Проверьте правильность `idInstance` и `apiTokenInstance` в `.env`
- Проверьте формат номера телефона
- Проверьте лимиты (100 сообщений/день для бесплатного тарифа)

### Ошибка при запуске:
- Убедитесь, что файл называется именно `.env` (не `.env.txt`)
- Убедитесь, что переменные начинаются с `VITE_`
- Перезапустите сервер после изменения `.env`

