# 🔴 КРИТИЧНО: Проверка Redirect URIs для Google и Facebook

## Проблема

После OAuth авторизации через Google/Facebook пользователь не определяется как авторизованный (`isSignedIn: false`). Это **почти всегда** означает, что Redirect URIs настроены неправильно.

## ✅ Правильная настройка Redirect URIs

### Шаг 1: Найдите ваш домен Clerk

1. Откройте [Clerk Dashboard](https://dashboard.clerk.com/)
2. Выберите ваше приложение
3. Перейдите в **Domains** (или **Settings** → **Domains**)
4. Найдите ваш домен разработки (development domain)
   - Формат: `ваше-приложение-XX.clerk.accounts.dev`
   - Пример: `meet-hound-54.clerk.accounts.dev`

### Шаг 2: Настройте Redirect URI для Google

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите ваш проект
3. Перейдите в **APIs & Services** → **Credentials**
4. Найдите ваш **OAuth 2.0 Client ID** (который используется для Clerk)
5. Нажмите на него для редактирования
6. В разделе **Authorized redirect URIs** добавьте:
   ```
   https://ВАШ_ДОМЕН_CLERK/v1/oauth_callback
   ```
   Например:
   ```
   https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback
   ```
7. **УДАЛИТЕ** все URI, которые указывают на `localhost` или ваш домен приложения
8. Сохраните изменения

### Шаг 3: Настройте Redirect URI для Facebook

1. Откройте [Facebook Developers](https://developers.facebook.com/)
2. Выберите ваше приложение
3. Перейдите в **Settings** → **Basic**
4. Прокрутите вниз до раздела **Valid OAuth Redirect URIs**
5. Добавьте:
   ```
   https://ВАШ_ДОМЕН_CLERK/v1/oauth_callback
   ```
   Например:
   ```
   https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback
   ```
6. **УДАЛИТЕ** все URI, которые указывают на `localhost` или ваш домен приложения
7. Сохраните изменения

## ❌ ЧАСТЫЕ ОШИБКИ

### НЕПРАВИЛЬНО:
- `http://localhost:5173`
- `http://localhost:5173/profile`
- `http://localhost:5173/callback`
- `https://ваш-сайт.com/callback`
- `https://ваш-сайт.com/profile`

### ПРАВИЛЬНО:
- `https://ваш-домен.clerk.accounts.dev/v1/oauth_callback`

## 🔍 Как проверить, что настройки правильные

1. После настройки Redirect URIs подождите 1-2 минуты (настройки могут применяться с задержкой)
2. Очистите кеш браузера и cookies
3. Попробуйте войти через Google/Facebook снова
4. Откройте консоль браузера (F12) и проверьте логи:
   - Должны появиться логи `ClerkAuthHandler: User authenticated`
   - `isSignedIn` должен стать `true`
   - Должен появиться объект `user`

## 📝 Дополнительная проверка в Clerk Dashboard

1. Откройте [Clerk Dashboard](https://dashboard.clerk.com/)
2. Перейдите в **User & Authentication** → **Social Connections**
3. Убедитесь, что Google и Facebook **включены** (переключатель активен)
4. Проверьте, что у них есть теги:
   - ✅ "Used for sign-in"
   - ✅ "Shared Credentials" (если нужно)

## ⚠️ Важно

- Redirect URIs должны указывать **ТОЛЬКО** на домен Clerk, НЕ на ваш сайт
- Clerk сам обрабатывает OAuth callback и затем перенаправляет пользователя на ваш сайт
- Если Redirect URIs указывают на localhost или ваш домен, OAuth не будет работать

## 🆘 Если все равно не работает

1. Проверьте, что в `.env` правильный `VITE_CLERK_PUBLISHABLE_KEY`
2. Убедитесь, что домен в Redirect URIs точно совпадает с доменом в Clerk Dashboard
3. Проверьте, что в Google/Facebook консоли нет опечаток в URI
4. Попробуйте создать новый OAuth приложение в Google/Facebook и использовать его
5. Проверьте логи в консоли браузера - там могут быть подсказки

