# 🚨 СРОЧНО: Google перенаправляет на localhost вместо Clerk

## Проблема
Google перенаправляет сразу на `localhost:5173/profile`, минуя Clerk домен. Это означает, что в Google Cloud Console в "Authorized redirect URIs" все еще есть localhost.

## ✅ КРИТИЧЕСКИЕ ШАГИ

### Шаг 1: Проверьте URL при авторизации в Google

1. Откройте DevTools (F12) → вкладка **Network**
2. Нажмите "Войти через Google"
3. Найдите запрос к `accounts.google.com` (обычно первый или второй)
4. Откройте его → вкладка **Headers**
5. Найдите **Request URL** - там должен быть параметр `redirect_uri`
6. **Скопируйте значение `redirect_uri`** - это покажет, куда Google пытается перенаправить

**Если там `http://localhost:5173`** - значит в Google Cloud Console все еще есть localhost!

### Шаг 2: Проверьте ВСЕ OAuth Client IDs

1. Откройте: https://console.cloud.google.com/apis/credentials
2. **ВНИМАТЕЛЬНО проверьте КАЖДЫЙ OAuth 2.0 Client ID**
3. Для КАЖДОГО:
   - Откройте его
   - Прокрутите до раздела **"Authorized redirect URIs"**
   - **ПОЛНОСТЬЮ УДАЛИТЕ** все строки с:
     - `http://localhost`
     - `http://localhost:5173`
     - `http://localhost:5173/profile`
     - `http://localhost:5173/callback`
     - Любые другие с `localhost`
   - **ДОБАВЬТЕ ТОЛЬКО ОДНУ СТРОКУ:**
     ```
     https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback
     ```
   - Нажмите **"SAVE"** внизу страницы
   - Дождитесь сообщения "OAuth client saved"

### Шаг 3: Проверьте Client ID в Clerk Dashboard

1. Откройте: https://dashboard.clerk.com/
2. **User & Authentication** → **Social Connections** → **Google**
3. Проверьте **Client ID** - он должен быть:
   ```
   1028494020168-034f525smlhane9lc4f041bbugokjt1i.apps.googleusercontent.com
   ```
4. Если Client ID другой:
   - Найдите этот Client ID в Google Cloud Console
   - Исправьте его Redirect URIs (удалите localhost, добавьте Clerk домен)

### Шаг 4: Полная очистка и ожидание

1. **Подождите 10-15 минут** после сохранения в Google Cloud Console
   - Настройки Google могут применяться с задержкой до 15 минут
   - Это критически важно!

2. **Полностью закройте браузер** (не только вкладки, а весь браузер)

3. **Очистите кеш и cookies:**
   - Нажмите `Ctrl+Shift+Delete`
   - Выберите "За все время" (All time)
   - Отметьте ВСЕ галочки
   - Нажмите "Удалить данные"

4. **Откройте браузер заново**

5. **Откройте приложение в режиме инкогнито:**
   - `Ctrl+Shift+N` (Chrome) или `Ctrl+Shift+P` (Firefox)
   - Откройте: `http://localhost:5173`

6. **Перезапустите dev-сервер:**
   ```bash
   # Остановите (Ctrl+C)
   npm run dev
   ```

### Шаг 5: Проверьте результат

1. Откройте DevTools (F12) → вкладка **Console**
2. Нажмите "Войти через Google"
3. Выберите аккаунт
4. **СРАЗУ посмотрите на URL в адресной строке:**
   - ✅ **ПРАВИЛЬНО:** `https://meet-hound-54.clerk.accounts.dev/...`
   - ❌ **НЕПРАВИЛЬНО:** `http://localhost:5173/profile`

5. Если все еще перенаправляет на localhost:
   - Проверьте Network tab - какой `redirect_uri` используется
   - Убедитесь, что в Google Cloud Console нет localhost
   - Подождите еще 10 минут

## 🔍 Диагностика через Network tab

1. Откройте DevTools (F12) → **Network**
2. Очистите список запросов (кнопка с крестиком)
3. Нажмите "Войти через Google"
4. Найдите запрос к `accounts.google.com` (обычно с параметрами `oauth`, `client_id`, `redirect_uri`)
5. Откройте его → вкладка **Payload** или **Query String Parameters**
6. Найдите параметр `redirect_uri`
7. **Этот URI должен быть:** `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
8. **Если там `http://localhost:5173`** - значит в Google Cloud Console все еще есть localhost!

## ⚠️ ВАЖНО

- Redirect URI в Google **НИКОГДА** не должен быть localhost
- Даже если вы думаете, что удалили его - проверьте еще раз
- Может быть несколько OAuth Client IDs - проверьте ВСЕ
- Настройки Google могут применяться до 15 минут

## 📝 Финальный чеклист

- [ ] Проверил Network tab - какой `redirect_uri` используется в запросе к Google
- [ ] Проверил ВСЕ OAuth Client IDs в Google Cloud Console
- [ ] Удалил ВСЕ URI с localhost из КАЖДОГО Client ID
- [ ] Оставил только `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
- [ ] Сохранил изменения и дождался сообщения "OAuth client saved"
- [ ] Подождал 10-15 минут
- [ ] Полностью закрыл браузер
- [ ] Очистил кеш и cookies
- [ ] Открыл в режиме инкогнито
- [ ] Перезапустил dev-сервер
- [ ] Проверил, что после выбора аккаунта попадаю на Clerk домен

