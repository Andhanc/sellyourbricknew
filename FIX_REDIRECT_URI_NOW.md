# 🔴 СРОЧНО: Исправление Redirect URI в Google

## Проблема
После выбора аккаунта Google вас сразу перенаправляет на `localhost:5173/profile`, минуя Clerk домен. Это означает, что Redirect URI в Google настроен неправильно.

## ✅ Решение

### Шаг 1: Откройте Google Cloud Console

1. Перейдите: https://console.cloud.google.com/apis/credentials
2. Найдите ваш **OAuth 2.0 Client ID** (который вы создали)
3. **Нажмите на него** для редактирования

### Шаг 2: Проверьте раздел "Authorized redirect URIs"

В этом разделе **НЕ ДОЛЖНО БЫТЬ:**
- ❌ `http://localhost:5173`
- ❌ `http://localhost:5173/profile`
- ❌ `http://localhost:5173/callback`
- ❌ Любые другие URI с `localhost`

**ДОЛЖЕН БЫТЬ ТОЛЬКО:**
- ✅ `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`

### Шаг 3: Исправьте Redirect URI

1. **УДАЛИТЕ** все URI, которые содержат `localhost`
2. **ДОБАВЬТЕ** правильный URI:
   ```
   https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback
   ```
3. Нажмите **"SAVE"** (Сохранить)

### Шаг 4: Проверьте раздел "Authorized JavaScript origins"

В этом разделе должно быть:
```
https://meet-hound-54.clerk.accounts.dev
```

**БЕЗ пути** `/v1/oauth_callback` и **БЕЗ слеша** в конце!

### Шаг 5: Подождите и очистите кеш

1. **Подождите 2-3 минуты** после сохранения (настройки применяются с задержкой)
2. **Очистите кеш браузера:**
   - Нажмите `Ctrl+Shift+Delete`
   - Выберите "Cookies" и "Cached images and files"
   - Очистите для `localhost` и `accounts.google.com`
3. В консоли браузера выполните: `sessionStorage.clear()`

### Шаг 6: Попробуйте снова

1. Перезапустите dev-сервер: `npm run dev`
2. Откройте: `http://localhost:5173`
3. Попробуйте войти через Google
4. **Проверьте URL в адресной строке:**
   - После выбора аккаунта Google вы **ДОЛЖНЫ** попасть на:
     ```
     https://meet-hound-54.clerk.accounts.dev/...
     ```
   - **НЕ** на `localhost:5173` сразу!

## 🔍 Как проверить, что исправлено

После нажатия "Войти через Google" и выбора аккаунта:

1. **Правильный поток:**
   - Google → `https://meet-hound-54.clerk.accounts.dev/...` → `localhost:5173/profile`
   
2. **Неправильный поток (сейчас у вас):**
   - Google → `localhost:5173/profile` (минуя Clerk)

## ⚠️ Важно

Redirect URI в Google **НИКОГДА** не должен указывать на localhost. Он должен указывать **ТОЛЬКО** на Clerk домен. Clerk сам обработает callback и перенаправит на localhost.

## 📝 Итоговый чеклист

- [ ] В Google Cloud Console открыт OAuth 2.0 Client ID
- [ ] В "Authorized redirect URIs" удалены все URI с `localhost`
- [ ] В "Authorized redirect URIs" добавлен: `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
- [ ] В "Authorized JavaScript origins" добавлен: `https://meet-hound-54.clerk.accounts.dev` (без пути)
- [ ] Нажата кнопка "SAVE"
- [ ] Подождали 2-3 минуты
- [ ] Очистили кеш браузера
- [ ] Очистили sessionStorage
- [ ] Перезапустили dev-сервер
- [ ] Попробовали войти снова

