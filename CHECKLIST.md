# ✅ Чеклист проверки настройки WhatsApp авторизации

## 📋 Что нужно проверить:

### 1. Файл .env создан и заполнен ✅

Убедитесь, что в корне проекта есть файл `.env` со следующим содержимым:

```env
VITE_GREEN_API_URL=https://7105.api.greenapi.com
VITE_GREEN_API_ID=7105458006
VITE_GREEN_API_TOKEN=cd0b90b23ee340cba0e7a90767355c44af7da8df155342a8bb
```

**Проверка:**
- [ ] Файл `.env` существует в корне проекта
- [ ] Все три переменные заполнены
- [ ] Нет лишних пробелов вокруг знака `=`
- [ ] Нет кавычек вокруг значений

### 2. WhatsApp подключен в Green API ✅

**Проверка:**
- [ ] Откройте панель Green API
- [ ] Статус инстанса должен быть **"Авторизован"** (не "Неавторизован")
- [ ] Если статус "Неавторизован" - подключите через QR-код

### 3. Сервер перезапущен ✅

**Проверка:**
- [ ] Остановите текущий сервер (Ctrl+C)
- [ ] Запустите снова: `npm run dev`
- [ ] Сервер запустился без ошибок

### 4. Код проверен ✅

Все файлы на месте:
- [x] `src/services/authService.js` - основная логика
- [x] `src/components/WhatsAppVerificationModal.jsx` - модальное окно
- [x] `src/components/LoginModal.jsx` - главное окно входа
- [x] `src/components/GoogleLoginButton.jsx` - кнопка Google (опционально)

### 5. Тестирование ✅

**Проверка работы:**

1. [ ] Откройте сайт: `http://localhost:5173`
2. [ ] Нажмите "Войти" или "Регистрация"
3. [ ] Нажмите "Войти через WhatsApp"
4. [ ] Введите номер телефона (тот, который подключен к Green API)
5. [ ] Нажмите "Отправить код"
6. [ ] Проверьте WhatsApp - должен прийти код
7. [ ] Введите код на сайте
8. [ ] Должна произойти авторизация и перенаправление на `/profile`

## ⚠️ Возможные проблемы:

### Проблема: Код не приходит в WhatsApp

**Решения:**
1. Проверьте статус в Green API - должен быть "Авторизован"
2. Проверьте правильность `idInstance` и `apiTokenInstance` в `.env`
3. Проверьте формат номера телефона (должен быть `79991234567`)
4. Проверьте лимиты (100 сообщений/день для бесплатного тарифа)
5. Попробуйте изменить URL в `.env` на:
   ```env
   VITE_GREEN_API_URL=https://api.green-api.com
   ```

### Проблема: Ошибка в консоли браузера

**Решения:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки
3. Убедитесь, что сервер перезапущен после создания `.env`
4. Проверьте, что переменные начинаются с `VITE_`

### Проблема: Белый экран

**Решения:**
1. Проверьте консоль браузера на ошибки
2. Проверьте консоль терминала (где запущен `npm run dev`)
3. Убедитесь, что все зависимости установлены: `npm install`

## 🎯 Правильный формат URL для Green API

Green API может использовать два формата URL:

**Вариант 1 (общий):**
```env
VITE_GREEN_API_URL=https://api.green-api.com
```

**Вариант 2 (специфичный для инстанса):**
```env
VITE_GREEN_API_URL=https://7105.api.greenapi.com
```

Если первый вариант не работает, попробуйте второй, и наоборот.

## ✅ Если все проверки пройдены:

Поздравляю! Авторизация через WhatsApp должна работать! 🎉

Если что-то не работает, проверьте консоль браузера (F12) и консоль терминала для ошибок.

