# 🚀 Быстрая настройка Dev Tunnels для тестирования

## Проблема
Фронтенд доступен через dev tunnels, но бэкенд недоступен для внешних тестировщиков.

## ✅ Решение (3 шага)

### Шаг 1: Создать dev tunnel для бэкенда

**В VS Code:**
1. Запустите бэкенд: `npm run server` (в отдельном терминале)
2. Нажмите `Ctrl+Shift+P` → введите "Ports: Focus on Ports View"
3. Найдите порт `3000` в списке
4. Нажмите на иконку справа от порта 3000
5. Выберите **"Port Visibility"** → **"Public"**
6. Скопируйте полученный URL (например: `https://xxxxx-3000.euw.devtunnels.ms`)

**Или через командную строку:**
```bash
# Убедитесь, что бэкенд запущен на порту 3000
npm run server

# В другом терминале создайте tunnel
devtunnels host -p 3000 --allow-anonymous
```

### Шаг 2: Настроить переменную окружения

Откройте файл `.env.local` в корне проекта и добавьте:

```env
# Замените xxxxx-3000 на ваш реальный URL из шага 1
VITE_API_BASE_URL=https://xxxxx-3000.euw.devtunnels.ms/api
```

**Пример:**
```env
VITE_API_BASE_URL=https://abc123-3000.euw.devtunnels.ms/api
```

### Шаг 3: Перезапустить фронтенд

```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
npm run dev
```

## ✅ Готово!

Теперь:
- Фронтенд доступен: `https://5f5ntx8k-5173.euw.devtunnels.ms/`
- Бэкенд доступен: `https://xxxxx-3000.euw.devtunnels.ms/api`
- Фронтенд автоматически обращается к бэкенду через dev tunnels

## 🔍 Проверка

1. Откройте фронтенд в браузере
2. Откройте DevTools (F12) → вкладка **Network**
3. Выполните любое действие (например, вход)
4. Проверьте, что запросы идут на ваш dev tunnel URL (не на localhost)

## ❗ Важно

- **Оба dev tunnels должны быть запущены одновременно**
- **Бэкенд должен быть запущен** (`npm run server`)
- **Фронтенд должен быть запущен** (`npm run dev`)
- После изменения `.env.local` **обязательно перезапустите фронтенд**

## 🐛 Проблемы?

### API запросы идут на localhost
- Проверьте, что переменная `VITE_API_BASE_URL` установлена в `.env.local`
- Перезапустите фронтенд после изменения `.env.local`

### CORS ошибки
- Убедитесь, что обновлен `server/server.js` (CORS уже настроен)
- Перезапустите бэкенд

### Dev tunnel не работает
- Убедитесь, что бэкенд запущен на порту 3000
- Проверьте, что dev tunnel создан для правильного порта
- Попробуйте пересоздать dev tunnel





