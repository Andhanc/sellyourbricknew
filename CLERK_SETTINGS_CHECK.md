# 🔧 Проверка настроек в Clerk Dashboard

## Проблема
`redirect_uri` правильный, но после OAuth редиректа Clerk не определяет пользователя (`isSignedIn: false`).

## ✅ Что проверить в Clerk Dashboard

### 1. Settings → Paths (Пути)

1. Откройте: https://dashboard.clerk.com/
2. Выберите ваше приложение
3. Перейдите: **Settings** → **Paths** (или **Paths** в меню)
4. Проверьте настройки:
   - **After sign-in URL**: `/profile` или оставьте пустым (будет использоваться из кода)
   - **After sign-up URL**: `/profile` или оставьте пустым
   - **After sign-out URL**: `/` или оставьте пустым

### 2. Settings → General (Общие настройки)

1. Перейдите: **Settings** → **General**
2. Проверьте:
   - **Allowed origins** - должен быть `http://localhost:5173` (или автоматически разрешен для разработки)
   - **Allowed redirect URLs** - должен быть `http://localhost:5173/profile` (или автоматически разрешен)

### 3. User & Authentication → Social Connections → Google

1. Перейдите: **User & Authentication** → **Social Connections** → **Google**
2. Убедитесь:
   - ✅ **"Use custom credentials"** включен
   - ✅ **Client ID** правильный
   - ✅ **Client Secret** правильный
   - ✅ **"Authorized Redirect URI"**: `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
   - ✅ **"Enable for sign-up and sign-in"** включен

### 4. Проверка через Network tab

После выбора аккаунта Google проверьте в Network tab:

1. **Редирект на Clerk домен:**
   - Должен быть запрос к `https://meet-hound-54.clerk.accounts.dev/...`
   - Статус должен быть 200 или 302

2. **Редирект обратно на localhost:**
   - Должен быть запрос к `http://localhost:5173/profile`
   - Проверьте, есть ли параметры в URL (например, `?__clerk_...` или `#__clerk_...`)

3. **Ошибки:**
   - Если есть запросы с ошибками (400, 500), откройте их и проверьте Response

### 5. Проверка cookies

1. Откройте DevTools (F12) → **Application** → **Cookies**
2. Проверьте cookies для:
   - `http://localhost:5173` - должны быть cookies от Clerk (например, `__clerk_...`)
   - `https://meet-hound-54.clerk.accounts.dev` - должны быть cookies после OAuth

### 6. Проверка консоли на ошибки

В консоли браузера проверьте:
- Ошибки CORS
- Ошибки блокировки cookies
- Ошибки от Clerk SDK

## 🔍 Диагностика

После нажатия "Войти через Google" и выбора аккаунта:

1. **Проверьте URL в адресной строке:**
   - После выбора аккаунта вы должны попасть на: `https://meet-hound-54.clerk.accounts.dev/...`
   - Затем редирект на: `http://localhost:5173/profile`
   - Если на `/profile` есть параметры типа `?__clerk_...` или `#__clerk_...` - это хорошо

2. **Проверьте Network tab:**
   - Найдите запросы к `clerk.accounts.dev` после редиректа от Google
   - Проверьте статус-коды и Response

3. **Проверьте cookies:**
   - Должны быть cookies от Clerk после редиректа

## ⚠️ Важно

Для разработки на localhost обычно **не нужно** настраивать Satellite domains или добавлять localhost в специальные списки - Clerk автоматически разрешает localhost для разработки.

Проблема скорее всего в том, что:
1. Clerk обрабатывает callback, но не может установить сессию
2. Или есть проблема с cookies/CORS
3. Или настройки в Clerk Dashboard блокируют что-то


