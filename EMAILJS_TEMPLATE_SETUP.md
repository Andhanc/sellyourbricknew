# 📝 Настройка шаблона EmailJS "One-Time Password"

## Что нужно сделать в интерфейсе EmailJS:

### 1. Поле "To Email" (правая панель)
- **Оставьте как есть:** `{{email}}`
- Это правильная переменная, наш код её поддерживает

### 2. Поле "Subject" (левая панель, вкладка Content)
- **Измените на:** `Код подтверждения регистрации`
- Или оставьте как есть, если вас устраивает текущий текст

### 3. Переменная кода в контенте (левая панель)
- **Оставьте как есть:** `{{passcode}}`
- Наш код автоматически подставит код в эту переменную

### 4. Переменная времени (если есть)
- **Оставьте как есть:** `{{time}}`
- Наш код автоматически подставит время истечения кода

### 5. Настройка "From Email" (правая панель)
- Убедитесь, что стоит галочка "Use Default Email Address"
- Или укажите ваш email адрес

### 6. Сохраните шаблон
- Нажмите синюю кнопку **"Save"** в правом верхнем углу

## Важно:

✅ **Не нужно менять переменные** `{{email}}` и `{{passcode}}` - они уже правильные!

✅ Наш код автоматически подставит:
- `{{email}}` → email получателя
- `{{passcode}}` → 6-значный код подтверждения
- `{{time}}` → время истечения кода (через 10 минут)

## После сохранения:

1. Скопируйте **Template ID** (будет показан после сохранения)
2. Добавьте его в `.env.local`:
   ```env
   VITE_EMAILJS_TEMPLATE_ID=template_xxxxxxx
   ```
3. Перезапустите сервер: `npm run dev`

Готово! Теперь коды будут отправляться на email! 🎉

