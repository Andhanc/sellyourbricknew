# 📋 Пояснение про порты: почему 8080?

## ❓ Вопрос:
Почему везде пишется про порт 8080, если фронт на 5173, а бэк на 3000?

## ✅ Ответ:

### 🔵 Локальная разработка (ваш компьютер):
- **Vite (фронтенд)**: порт `5173` ✅
- **Сервер (бэкенд)**: порт `3000` ✅

### 🟢 На Railway (production):
- **Vite (фронтенд)**: порт `PORT` (Railway устанавливает автоматически, например `8080`) ✅
- **Сервер (бэкенд)**: порт `SERVER_PORT` (нужно установить `3000` в Railway Variables) ✅

## 🔍 Почему 8080?

**8080 - это просто пример!** Railway может установить любой порт:
- `8080` (часто используется)
- `3000` (тоже часто)
- `5000`, `8000`, или любой другой

**Важно:** Railway сам выбирает порт и устанавливает его в переменную `PORT`.

## 📊 Как это работает:

### В коде (`vite.config.js`):
```javascript
// Локально: PORT не установлен → используем 5173
// На Railway: PORT установлен Railway → используем PORT (например 8080)
const vitePort = process.env.PORT ? parseInt(process.env.PORT) : 5173
```

### В коде (`server/server.js`):
```javascript
// Всегда используем SERVER_PORT или 3000
// Локально: SERVER_PORT не установлен → используем 3000
// На Railway: SERVER_PORT установлен вручную → используем SERVER_PORT (3000)
const PORT = process.env.SERVER_PORT || 3000
```

## 🎯 Итого:

| Среда | Vite (фронт) | Сервер (бэк) |
|-------|--------------|--------------|
| **Локально** | `5173` | `3000` |
| **Railway** | `PORT` (8080 или другой) | `SERVER_PORT` (3000) |

## 💡 Почему так сделано?

1. **Railway управляет портами** - он сам выбирает свободный порт для вашего сервиса
2. **Vite должен слушать на PORT** - чтобы Railway мог направить трафик на ваше приложение
3. **Сервер на отдельном порту** - чтобы Vite мог проксировать запросы `/api/*` на сервер

## ✅ Что нужно сделать:

В Railway Dashboard → Settings → Variables установите:
- `SERVER_PORT=3000` ← **ОБЯЗАТЕЛЬНО**

Railway автоматически установит:
- `PORT=8080` (или другой порт) ← **НЕ нужно устанавливать вручную!**

## 🔧 В Dockerfile:

```dockerfile
EXPOSE 8080
```

**Это просто пример/документация!** Реальный порт определяется переменной `PORT` от Railway.

---

**Вывод:** 8080 - это не жестко заданный порт, а просто пример того, что Railway может установить. В коде все правильно настроено для работы с любым портом, который установит Railway.
