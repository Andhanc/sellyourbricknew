# Руководство по деплою с Dockerfile на Railway

## ✅ Что было сделано:

1. **Создан `Dockerfile`** - полный контроль над сборкой
2. **Создан `.dockerignore`** - исключает ненужные файлы
3. **Удален `nixpacks.toml`** - Railway будет использовать Dockerfile

## 🐳 Преимущества Dockerfile:

- ✅ Все системные библиотеки устанавливаются явно
- ✅ Полный контроль над процессом сборки
- ✅ Меньше проблем с зависимостями
- ✅ Более предсказуемое поведение
- ✅ Все библиотеки для Chrome/Puppeteer установлены

## 📋 Что делает Dockerfile:

1. Использует `node:20-slim` как базовый образ
2. Устанавливает системные зависимости:
   - Python3, make, g++ (для better-sqlite3)
   - Все библиотеки для Chrome/Puppeteer (libglib2.0-0 и т.д.)
   - procps (для concurrently)
3. Устанавливает npm зависимости через `npm ci`
4. Копирует код приложения
5. Запускает `npm start` (оба процесса через concurrently)

## 🔧 Переменные окружения в Railway:

**ОБЯЗАТЕЛЬНО установите в Railway Dashboard → Settings → Variables:**
- `SERVER_PORT=3000` ← **КРИТИЧЕСКИ ВАЖНО!**

**Автоматически устанавливается Railway (НЕ добавляйте вручную):**
- `PORT` - Railway установит автоматически

## 🚀 Как Railway определит Dockerfile:

Railway автоматически обнаружит `Dockerfile` в корне проекта и будет использовать его вместо nixpacks.

Если хотите явно указать:
- Railway Dashboard → Settings → Build
- Выберите "Dockerfile" как метод сборки

## ✅ Проверка после деплоя:

В логах должны быть:
- ✅ `VITE v5.x.x ready` на порту PORT (Railway установит)
- ✅ `🚀 Сервер запущен на порту 3000`
- ✅ Оба процесса работают
- ✅ WhatsApp может запуститься (библиотеки установлены)

## 🔄 Если нужно вернуться к nixpacks:

1. Удалите `Dockerfile`
2. Создайте `nixpacks.toml` заново
3. Railway автоматически переключится на nixpacks

## 📝 Следующие шаги:

1. Закоммитьте `Dockerfile` и `.dockerignore`
2. Установите `SERVER_PORT=3000` в Railway Variables
3. Задеплойте снова
4. Railway автоматически использует Dockerfile
