# 🚨 КРИТИЧНО: Google все еще перенаправляет на localhost

## Проблема
После нажатия "Продолжить" на странице Google вас сразу перенаправляет на `localhost:5173/profile`, минуя Clerk домен. Это означает, что в Google Cloud Console в "Authorized redirect URIs" **ВСЕ ЕЩЕ ЕСТЬ localhost**.

## ✅ КРИТИЧЕСКИЕ ШАГИ

### Шаг 1: Проверьте ВСЕ OAuth Client IDs

1. Откройте: https://console.cloud.google.com/apis/credentials
2. **ВНИМАТЕЛЬНО проверьте КАЖДЫЙ OAuth 2.0 Client ID** в списке
3. **Для КАЖДОГО Client ID:**
   - Откройте его (нажмите на название)
   - Прокрутите до раздела **"Authorized redirect URIs"**
   - **ПОЛНОСТЬЮ УДАЛИТЕ ВСЕ строки**, которые содержат:
     - `localhost`
     - `127.0.0.1`
     - `http://localhost`
     - `http://localhost:5173`
     - `http://localhost:5173/profile`
     - `http://localhost:5173/callback`
     - Любые другие с `localhost` или `127.0.0.1`
   - **ДОБАВЬТЕ ТОЛЬКО ОДНУ СТРОКУ:**
     ```
     https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback
     ```
   - Нажмите **"SAVE"** внизу страницы
   - Дождитесь сообщения "OAuth client saved"

### Шаг 2: Убедитесь, что используется правильный Client ID

1. В Clerk Dashboard: **User & Authentication** → **Social Connections** → **Google**
2. Проверьте **Client ID** - он должен быть:
   ```
   1028494020168-034f525smlhane9lc4f041bbugokjt1i.apps.googleusercontent.com
   ```
3. Если Client ID другой:
   - Найдите этот Client ID в Google Cloud Console
   - Исправьте его Redirect URIs (удалите localhost, добавьте Clerk домен)

### Шаг 3: Полная очистка и ожидание

1. **Подождите 15-20 минут** после сохранения в Google Cloud Console
   - Настройки Google могут применяться с задержкой до 20 минут
   - Это критически важно!

2. **Полностью закройте браузер** (не только вкладки, а весь браузер)

3. **Очистите кеш и cookies:**
   - Нажмите `Ctrl+Shift+Delete`
   - Выберите "За все время" (All time)
   - Отметьте ВСЕ галочки
   - Нажмите "Удалить данные"

4. **Откройте браузер заново**

5. **Откройте приложение в режиме инкогнито:**
   - `Ctrl+Shift+N` (Chrome) или `Ctrl+Shift+P` (Firefox)
   - Откройте: `http://localhost:5173`

6. **Перезапустите dev-сервер:**
   ```bash
   # Остановите (Ctrl+C)
   npm run dev
   ```

### Шаг 4: Проверьте результат

1. Откройте DevTools (F12) → вкладка **Network**
2. Очистите список запросов
3. Нажмите "Войти через Google"
4. Выберите аккаунт
5. Нажмите "Продолжить"
6. **СРАЗУ посмотрите на URL в адресной строке:**
   - ✅ **ПРАВИЛЬНО:** `https://meet-hound-54.clerk.accounts.dev/...`
   - ❌ **НЕПРАВИЛЬНО:** `http://localhost:5173/profile`

## 🔍 Дополнительная проверка

### Проверьте через Network tab

После нажатия "Войти через Google":

1. Найдите запрос к `accounts.google.com` (с параметрами OAuth)
2. Откройте его → вкладка **Payload** или **Query String Parameters**
3. Найдите параметр `redirect_uri`
4. **Этот URI должен быть:** `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
5. **Если там `http://localhost:5173`** - значит в Google Cloud Console все еще есть localhost!

## ⚠️ КРИТИЧЕСКИ ВАЖНО

- Redirect URI в Google **НИКОГДА** не должен быть localhost
- Даже если вы думаете, что удалили его - проверьте еще раз
- Может быть несколько OAuth Client IDs - проверьте **ВСЕ**
- Настройки Google могут применяться до 20 минут

## 📝 Чеклист

- [ ] Проверил ВСЕ OAuth Client IDs в Google Cloud Console
- [ ] Удалил ВСЕ URI с localhost из КАЖДОГО Client ID
- [ ] Оставил только `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
- [ ] Сохранил изменения и дождался "OAuth client saved"
- [ ] Подождал 15-20 минут
- [ ] Полностью закрыл браузер
- [ ] Очистил кеш и cookies
- [ ] Открыл в режиме инкогнито
- [ ] Перезапустил dev-сервер
- [ ] Проверил через Network tab - какой `redirect_uri` используется
- [ ] Проверил, что после "Продолжить" попадаю на Clerk домен


