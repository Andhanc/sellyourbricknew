# Проверка логов для диагностики проблемы

## Критически важно:

После того как build проходит успешно, Railway запускает `npm start`. 

**Нужно проверить логи запуска приложения:**

1. В Railway Dashboard откройте **View logs**
2. Прокрутите вниз до момента после "Build time: XX seconds"
3. Найдите логи после запуска `npm start`

## Что должно быть в логах:

### ✅ Если все правильно, вы должны увидеть:

**От SERVER процесса (синий текст):**
```
[SERVER] 🚀 Сервер запущен на порту 3000
[SERVER] 📡 API доступен по адресу: http://0.0.0.0:3000/api
[SERVER] 🌐 Railway PORT: [номер]
[SERVER] 🔧 SERVER_PORT: 3000
```

**От FRONTEND процесса (зеленый текст):**
```
[FRONTEND] VITE v5.x.x  ready in xxx ms
[FRONTEND] ➜  Local:   http://0.0.0.0:[PORT]/
[FRONTEND] ➜  Network: use --host to expose
```

### ❌ Если что-то не так:

1. **Если видите ошибки** - пришлите их полностью
2. **Если один из процессов не запускается** - пришлите логи
3. **Если процессы запускаются, но падают** - пришлите ошибки

## Возможные проблемы:

### Проблема 1: Процессы не запускаются
- Проверьте, что `SERVER_PORT=3000` установлено в Variables
- Проверьте логи на наличие ошибок при запуске

### Проблема 2: Railway не может подключиться
- Railway ожидает, что приложение слушает на `PORT`
- Vite должен слушать на `PORT` (Railway установит автоматически)
- Проверьте, что Vite действительно запустился

### Проблема 3: Таймаут при запуске
- Если процессы запускаются слишком долго, Railway может не дождаться
- Проверьте, нет ли долгих операций при старте (инициализация БД и т.д.)

## Что сделать:

1. **Откройте View logs в Railway Dashboard**
2. **Найдите логи после "Build time"**
3. **Скопируйте все логи запуска приложения**
4. **Пришлите их мне** - я смогу определить проблему

Без логов запуска невозможно понять, что именно происходит при деплое.
