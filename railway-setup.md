# Настройка проекта для Railway (Вариант 2: Один сервис с concurrently)

## ✅ Что было сделано:

1. ✅ Установлен `concurrently` в devDependencies
2. ✅ Добавлен скрипт `start` в package.json для запуска обоих процессов
3. ✅ Создан `Procfile` для Railway
4. ✅ Настроены порты:
   - **Vite (фронтенд)**: использует `PORT` (Railway автоматически установит)
   - **Server (бэкенд)**: использует `SERVER_PORT` (3000 по умолчанию)
5. ✅ Настроен прокси в Vite для маршрутизации `/api/*` на бэкенд

## 📋 Переменные окружения для Railway:

В настройках Railway (Settings → Variables) добавьте:

### Обязательные:
- `SERVER_PORT=3000` - порт для бэкенд-сервера (важно!)

### Опциональные:
- `API_URL=http://localhost:3000` - URL API (по умолчанию используется localhost:3000)
- Все остальные переменные окружения вашего приложения (Clerk, Google, EmailJS и т.д.)

## 🚀 Как это работает:

1. Railway автоматически определяет `Procfile` и запускает `npm start`
2. `concurrently` запускает оба процесса параллельно:
   - **SERVER** (синий) - `npm run server` → бэкенд на порту `SERVER_PORT` (3000)
   - **FRONTEND** (зеленый) - `npm run dev` → фронтенд на порту `PORT` (устанавливает Railway)
3. Vite dev сервер проксирует все запросы `/api/*` на бэкенд-сервер

## 🔧 Архитектура:

```
Пользователь → Railway URL (PORT) → Vite Dev Server
                                      ↓
                                   /api/* → Backend Server (SERVER_PORT: 3000)
```

## ⚠️ Важно:

- **Файл БД** (`server/database.sqlite`) должен быть в `.gitignore` ✅
- Убедитесь, что БД не больше 80 МБ перед деплоем
- На Railway БД будет создаваться автоматически при первом запуске
- Если БД уже была закоммичена, удалите её из истории Git (см. ниже)

## 📝 Проверка перед деплоем:

### Локальная проверка:
```bash
# Установите зависимости (если еще не установлены)
npm install

# Проверьте что все работает локально
npm start
# Должны запуститься оба процесса:
# - Сервер на http://localhost:3000
# - Vite на http://localhost:5173
```

### После деплоя на Railway:
- ✅ Фронтенд будет доступен на основном URL Railway (например: `https://your-app.railway.app`)
- ✅ API будет доступно через `/api/*` (проксируется Vite на бэкенд)
- ✅ Проверьте логи в Railway Dashboard для отладки

## 🗑️ Удаление БД из Git (если нужно):

Если файл БД уже был закоммичен:

```bash
# 1. Удалить из индекса
git rm --cached server/database.sqlite

# 2. Закоммитить
git commit -m "Remove database.sqlite from git"

# 3. Удалить из истории (опционально, но рекомендуется)
$env:FILTER_BRANCH_SQUELCH_WARNING=1
git filter-branch --force --index-filter "git rm --cached --ignore-unmatch server/database.sqlite" --prune-empty --tag-name-filter cat -- --all

# 4. Очистить резервные копии
rm -rf .git/refs/original/
git reflog expire --expire=now --all
git gc --prune=now --aggressive

# 5. Отправить изменения
git push origin --force --all
```

## 📦 Структура файлов:

- `Procfile` - указывает Railway запускать `npm start`
- `package.json` - содержит скрипт `start` с concurrently
- `vite.config.js` - настроен прокси для `/api/*`
- `server/server.js` - использует `SERVER_PORT` для порта
