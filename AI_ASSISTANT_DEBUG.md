# Диагностика умного помощника

## Что было исправлено:

1. ✅ Добавлено подробное логирование всех запросов к AI
2. ✅ Увеличен таймаут для запросов с 30 до 90 секунд (для сложных запросов)
3. ✅ Добавлена автоматическая повторная попытка при таймауте
4. ✅ Оптимизирована история сообщений (ограничена до последних 10 сообщений)
5. ✅ Улучшена обработка ошибок с понятными сообщениями
6. ✅ Добавлена обработка различных типов ошибок:
   - 401 - неверный API ключ
   - 429 - превышен лимит запросов
   - 500+ - ошибка сервера
   - Таймаут запроса (с автоматической повторной попыткой)
   - Ошибки сети
   - Ошибки CORS

## Как проверить, что не работает:

1. Откройте консоль браузера (F12 → Console)
2. Попробуйте отправить сообщение умному помощнику
3. Проверьте логи в консоли:
   - `🤖 Отправка запроса к AI сервису...` - запрос отправлен
   - `✅ Получен ответ от AI сервиса` - ответ получен
   - `❌ API Error` - ошибка API
   - `🔑 Ошибка авторизации` - проблема с API ключом
   - `⏱️ Превышен лимит запросов` - слишком много запросов
   - `🌐 Ошибка сети` - проблема с интернетом

## Возможные проблемы и решения:

### 1. Ошибка 401 (неверный API ключ)
**Решение:** API ключ истек или недействителен. Нужно обновить ключ в файле `src/services/aiService.js`

### 2. Ошибка 429 (превышен лимит)
**Решение:** Слишком много запросов. Подождите несколько минут и попробуйте снова.

### 3. Ошибка сети
**Решение:** 
- Проверьте подключение к интернету
- Проверьте, не блокирует ли брандмауэр/антивирус запросы
- Проверьте, доступен ли API: `https://api.intelligence.io.solutions`

### 4. Таймаут запроса
**Решение:** 
- Таймаут увеличен до 90 секунд
- Добавлена автоматическая повторная попытка при таймауте
- Если проблема сохраняется:
  - Упростите вопрос
  - Разбейте сложный вопрос на несколько простых
  - Попробуйте позже (возможно, сервер AI перегружен)

### 5. Ошибка CORS
**Решение:** Проблема с настройками CORS на сервере API. Нужно связаться с администратором API.

## Проверка API ключа:

API ключ находится в файле `src/services/aiService.js` на строке 3.

Если ключ истек, нужно:
1. Получить новый ключ от провайдера API
2. Заменить старый ключ на новый в файле `src/services/aiService.js`

## Тестирование:

После исправлений попробуйте:
1. Открыть страницу с умным помощником
2. Отправить простое сообщение (например, "Привет")
3. Проверить консоль браузера на наличие ошибок
4. Проверить, получен ли ответ от AI

Если проблема сохраняется, проверьте логи в консоли и сообщите администратору о конкретной ошибке.

