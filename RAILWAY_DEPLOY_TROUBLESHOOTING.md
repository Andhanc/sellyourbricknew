# Решение проблемы "Configure network" на Railway

## Проблема:
Деплой падает на этапе "Deploy > Configure network", даже если домен настроен.

## Возможные причины и решения:

### 1. Проверьте логи в Railway Dashboard
Перейдите в **View logs** и проверьте:
- ✅ Оба процесса запускаются:
  - `🚀 Сервер запущен на порту 3000` (от SERVER)
  - `VITE v5.x.x ready in xxx ms` (от FRONTEND)
- ❌ Если видите ошибки - пришлите их

### 2. Переменные окружения
В Railway Dashboard → Settings → Variables убедитесь:
- `SERVER_PORT=3000` - **ОБЯЗАТЕЛЬНО**
- `PORT` - Railway установит автоматически (не нужно добавлять вручную)

### 3. Проверка портов
Railway ожидает, что приложение слушает на `PORT` (который Railway устанавливает автоматически).
- **Vite (фронтенд)**: должен слушать на `PORT` ✅
- **Server (бэкенд)**: должен слушать на `SERVER_PORT` (3000) ✅
- Оба должны слушать на `0.0.0.0` ✅

### 4. Если проблема сохраняется:

#### Вариант A: Проверьте, что процессы запускаются
В логах должны быть сообщения от обоих процессов. Если одного нет - проблема в запуске.

#### Вариант B: Временное решение - один процесс
Если проблема критична, можно временно использовать только сервер с раздачей статики:
1. Соберите фронтенд: `npm run build`
2. Настройте Express для раздачи статики из `dist`
3. Запускайте только `npm run server`

#### Вариант C: Проверьте Railway Health Checks
Railway может проверять health endpoint. Убедитесь, что:
- Приложение отвечает на запросы
- Нет ошибок при старте
- Оба процесса не падают

### 5. Что было исправлено:
- ✅ Сервер слушает на `0.0.0.0`
- ✅ Vite слушает на `0.0.0.0`
- ✅ Добавлена обработка ошибок
- ✅ Улучшено логирование для диагностики
- ✅ Добавлен `--kill-others-on-fail` в concurrently

### 6. Следующие шаги:
1. Закоммитьте изменения
2. Задеплойте снова
3. Проверьте логи - должны быть сообщения от обоих процессов
4. Если ошибка повторится - пришлите полные логи из Railway
