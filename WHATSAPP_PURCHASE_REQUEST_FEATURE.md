# Функционал отправки WhatsApp сообщений при запросе на покупку

## ✅ Что было сделано

Добавлен автоматический функционал отправки WhatsApp сообщения покупателю после создания запроса на покупку объекта недвижимости.

## 📋 Описание функционала

### Как это работает:

1. **Пользователь отправляет запрос на покупку** через модальное окно "Купить сейчас" (`BuyNowModal.jsx`)
2. **Сервер создает запрос** в базе данных через endpoint `POST /api/purchase-requests`
3. **Автоматически отправляется WhatsApp сообщение** покупателю с подтверждением

### Текст сообщения:

```
Вы отправили запрос на покупку объекта "(Название объекта)". Наш менеджер скоро с вами свяжется.
```

## 🔧 Технические детали

### Измененные файлы:

- **`server/server.js`** (строки 1594-1739)
  - Изменена функция `app.post('/api/purchase-requests')` с синхронной на асинхронную (`async`)
  - Добавлен блок кода для отправки WhatsApp сообщения после создания запроса
  - Сообщение отправляется асинхронно через `setImmediate()`, чтобы не блокировать основной ответ клиенту

### Логика работы:

1. **Проверка номера телефона**:
   - Проверяется наличие `buyerPhone` в данных запроса
   - Форматируется номер (удаляются все символы кроме цифр)
   - Проверяется длина номера (минимум 10 цифр)

2. **Проверка готовности WhatsApp клиента**:
   - Проверяется `waClientReady` (готовность клиента)
   - Проверяется `waClient` (наличие клиента)

3. **Отправка сообщения**:
   - Применяется патч `applySendSeenPatch()` для корректной работы
   - Отправляется сообщение через `waClient.sendMessage()`
   - Логируется результат (успех или ошибка)

### Обработка ошибок:

- Если номер телефона не указан → логируется информационное сообщение
- Если номер некорректный → логируется предупреждение
- Если WhatsApp клиент не готов → логируется предупреждение
- Если ошибка при отправке → логируется ошибка с деталями

**Важно**: Все ошибки обрабатываются внутри асинхронного блока, поэтому они **НЕ блокируют** создание запроса на покупку. Даже если WhatsApp сообщение не отправится, запрос все равно будет успешно создан.

## 🧪 Тестирование

### Как протестировать:

1. **Перезапустите сервер**:
   ```bash
   npm run server
   ```
   Остановите текущий сервер (Ctrl+C) и запустите заново, чтобы применить изменения.

2. **Отправьте запрос на покупку**:
   - Откройте любой объект недвижимости на сайте
   - Нажмите кнопку "Купить сейчас"
   - Заполните форму и нажмите "Отправить запрос на покупку"

3. **Проверьте логи сервера**:
   - В терминале должно появиться сообщение:
     ```
     ✅ Создан новый запрос на покупку: { id: X, buyer: '...', property: '...', ... }
     📱 WhatsApp сообщение отправлено покупателю: { phone: '...', buyer: '...', property: '...' }
     ```

4. **Проверьте WhatsApp**:
   - Покупатель должен получить сообщение в WhatsApp на указанный номер телефона

### Возможные сообщения в логах:

- ✅ **Успех**: `📱 WhatsApp сообщение отправлено покупателю:`
- ⚠️ **Нет номера**: `ℹ️ Номер телефона покупателя не указан. WhatsApp сообщение не отправлено:`
- ⚠️ **Некорректный номер**: `⚠️ Некорректный номер телефона покупателя. WhatsApp сообщение не отправлено:`
- ⚠️ **WhatsApp не готов**: `⚠️ WhatsApp клиент не готов. Сообщение покупателю не отправлено:`
- ❌ **Ошибка**: `❌ Ошибка отправки WhatsApp сообщения покупателю:`

## 📝 Примечания

### Где берется номер телефона покупателя?

Номер телефона передается в поле `buyerPhone` при создании запроса на покупку. Это значение берется из профиля пользователя, который отправляет запрос (через `getUserData()` в `BuyNowModal.jsx`).

### Что если у пользователя нет номера телефона?

Если у пользователя не указан номер телефона в профиле, WhatsApp сообщение просто не будет отправлено, но запрос на покупку все равно будет создан. В логах появится информационное сообщение.

### Можно ли изменить текст сообщения?

Да! Откройте файл `server/server.js`, найдите строку 1687 и измените текст:

```javascript
const whatsappMessage = `Вы отправили запрос на покупку объекта "${propertyTitle}". Наш менеджер скоро с вами свяжется.`;
```

## 🔗 Связанные файлы

- **Frontend**: `src/components/BuyNowModal.jsx` - модальное окно с формой запроса на покупку
- **Backend**: `server/server.js` - серверная логика создания запроса и отправки WhatsApp
- **Database**: `server/database/database.js` - функции работы с БД для запросов на покупку
- **Admin Panel**: `src/components/admin/Clients.jsx` - существующий функционал отправки WhatsApp

## ✅ Готово!

Функционал полностью реализован и готов к использованию. После перезапуска сервера при каждом создании запроса на покупку пользователь будет автоматически получать подтверждающее сообщение в WhatsApp.
