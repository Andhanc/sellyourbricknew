# Объяснение портов в проекте

## 🎯 Как работают порты:

### В локальной разработке:
- **Vite (фронтенд)**: порт `5173` (по умолчанию)
- **Server (бэкенд)**: порт `3000` (по умолчанию)

### На Railway (production):
- **Vite (фронтенд)**: порт `PORT` (Railway устанавливает автоматически, например 8080)
- **Server (бэкенд)**: порт `SERVER_PORT` (нужно установить `3000` в Railway Variables)

## 📋 Почему так:

1. **Railway автоматически устанавливает `PORT`** для основного веб-сервиса
   - Это может быть 8080, 3000, или любой другой порт
   - Railway направляет весь трафик на этот порт

2. **Vite должен слушать на `PORT`** (Railway установит)
   - Пользователи подключаются к Railway URL → Railway направляет на PORT → Vite

3. **Сервер слушает на `SERVER_PORT`** (3000)
   - Это внутренний порт
   - Vite проксирует запросы `/api/*` на сервер по этому порту

## 🔧 В Dockerfile:

```dockerfile
EXPOSE 8080
```

**Важно:** `EXPOSE` в Dockerfile - это только **метаданные** (документация), не реальный порт!

- Реальный порт определяется переменной `PORT`, которую устанавливает Railway
- Railway может установить PORT=8080, PORT=3000, или любой другой
- Vite автоматически использует `process.env.PORT` из конфигурации

## 📊 Схема работы:

```
Пользователь → Railway URL (например: https://your-app.railway.app)
                    ↓
              Railway направляет на PORT (8080)
                    ↓
              Vite слушает на PORT (8080) ✅
                    ↓
              Запрос /api/* → Vite проксирует → Сервер на SERVER_PORT (3000) ✅
```

## ✅ Что нужно установить в Railway:

**Settings → Variables:**
- `SERVER_PORT=3000` ← **ОБЯЗАТЕЛЬНО**

**Автоматически:**
- `PORT` - Railway установит (может быть 8080, 3000, или другой)

## 🎯 Итого:

- **EXPOSE 8080** в Dockerfile - это просто пример/документация
- **Реальный порт** определяется переменной `PORT` от Railway
- **Vite** использует `process.env.PORT` (Railway установит)
- **Сервер** использует `process.env.SERVER_PORT` (нужно установить 3000)
