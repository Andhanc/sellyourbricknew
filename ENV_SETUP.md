# 🔧 Настройка переменных окружения (.env.local)

## ✅ Поддержка обоих форматов

**Проект поддерживает оба формата переменных окружения:**
- `REACT_APP_*` (Create React App формат) - **рекомендуется**
- `VITE_*` (Vite формат) - также работает

## 📝 Создание .env.local файла

Создайте файл `.env.local` в корне проекта (рядом с `package.json`) со следующим содержимым:

```env
# ============================================
# CLERK AUTHENTICATION (ОБЯЗАТЕЛЬНО)
# ============================================
# Рекомендуется использовать REACT_APP_ формат (Create React App)
REACT_APP_CLERK_PUBLISHABLE_KEY=pk_test_bWVldC1ob3VuZC01NC5jbGVyay5hY2NvdW50cy5kZXYk

# ============================================
# ОПЦИОНАЛЬНЫЕ ПЕРЕМЕННЫЕ
# ============================================

# Google OAuth (для дополнительной интеграции)
REACT_APP_GOOGLE_CLIENT_ID=1028494020168-034f525smlhane9lc4fo41bbugokjt1i.apps.googleusercontent.com

# EmailJS для отправки email
REACT_APP_EMAILJS_SERVICE_ID=service_xxxxxxx
REACT_APP_EMAILJS_TEMPLATE_ID=template_xxxxxxx
REACT_APP_EMAILJS_PUBLIC_KEY=ваш_public_key

# API Base URL (по умолчанию /api)
REACT_APP_API_BASE_URL=/api

# WhatsApp Green API
REACT_APP_GREEN_API_URL=https://api.green-api.com
REACT_APP_GREEN_API_ID=ваш_id
REACT_APP_GREEN_API_TOKEN=ваш_токен
```

**Альтернативно:** Если хотите использовать формат Vite, используйте `VITE_*` префиксы:
```env
VITE_CLERK_PUBLISHABLE_KEY=pk_test_bWVldC1ob3VuZC01NC5jbGVyay5hY2NvdW50cy5kZXYk
VITE_GOOGLE_CLIENT_ID=ваш_google_client_id
# и т.д.
```

## ✅ Проверка после настройки

1. **Перезапустите сервер разработки** после создания/изменения `.env.local`:
   ```bash
   # Остановите текущий процесс (Ctrl+C)
   # Запустите снова
   npm run dev
   ```

2. **Проверьте в консоли браузера**, что ключ загружен:
   Откройте консоль разработчика (F12) и проверьте, нет ли ошибки:
   ```
   Missing Clerk Publishable Key. Please set REACT_APP_CLERK_PUBLISHABLE_KEY or VITE_CLERK_PUBLISHABLE_KEY in .env.local
   ```

## 🚨 Частые ошибки

### Ошибка 1: Не перезапустили сервер
- После изменения `.env.local` необходимо перезапустить `npm run dev`

### Ошибка 2: Файл называется .env вместо .env.local
- Используйте `.env.local` для локальной разработки
- Этот файл игнорируется Git (не попадает в репозиторий)

### Ошибка 3: Неправильный формат ключа
- Убедитесь, что ключ начинается с `pk_test_` или `pk_live_`
- Убедитесь, что нет лишних пробелов или символов

## 📍 Где получить ключи

### Clerk Publishable Key
1. Зайдите на https://dashboard.clerk.com
2. Выберите ваш проект (или создайте новый)
3. Перейдите в Settings → API Keys
4. Скопируйте **Publishable key** (начинается с `pk_test_` или `pk_live_`)

### Google Client ID
1. Зайдите в Google Cloud Console
2. Создайте проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 Client ID
5. Скопируйте Client ID

## 🔍 Проверка переменных в коде

Если хотите проверить, что переменные загружены, добавьте в код:

```javascript
import { getClerkPublishableKey } from './utils/env'

console.log('Clerk Key:', getClerkPublishableKey() ? 'Загружен' : 'НЕ ЗАГРУЖЕН')
```

**Важно:** 
- В Vite используется `import.meta.env`, в Create React App - `process.env`
- Проект автоматически поддерживает оба формата через утилиту `utils/env.js`
