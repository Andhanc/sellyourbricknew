# 🎨 Визуальное отображение системы бронирования

## ✅ Что реализовано

Полная визуальная система отображения статуса бронирования объектов недвижимости.

## 📍 Где отображается бронь

### 1. На карточках объектов (MainPage)

**Расположение**: Левый верхний угол изображения объекта

**Визуал**:
```
┌─────────────────────────┐
│ 🔒 Забронирован         │ ← Оранжевый бейдж
│                         │
│   [Изображение объекта] │
│                         │
└─────────────────────────┘
```

**Стиль**:
- Оранжевый градиент фон (#ffa726 → #fb8c00)
- Белый текст
- Иконка замка
- Анимация появления
- Тень для выделения

**Реализовано в**:
- `src/pages/MainPage.jsx` (строки 2137-2142, 2334-2339, 2534-2539)
- `src/pages/MainPage.css` (строки 1015-1035)

### 2. На странице детального просмотра (PropertyDetailClassic)

**Расположение**: Над блоком с ценой, под заголовком

**Визуал для своей брони** (зеленый):
```
┌────────────────────────────────────────┐
│ 🕐  Вы забронировали этот объект       │
│     Осталось: 71ч 45м                  │
└────────────────────────────────────────┘
```

**Визуал для чужой брони** (оранжевый):
```
┌────────────────────────────────────────┐
│ 🔒  Объект забронирован                │
│     Осталось: 71ч 45м                  │
│     Другие пользователи не могут       │
│     отправить запрос на покупку        │
└────────────────────────────────────────┘
```

**Стили**:
- **Своя бронь**: Зеленый градиент (#0ABAB5 → #089a95)
- **Чужая бронь**: Оранжевый градиент (#ffa726 → #fb8c00)
- Пульсирующая иконка часов
- Таймер обратного отсчета (обновляется каждую секунду)
- Автоматическое исчезновение при истечении времени

**Реализовано в**:
- `src/components/ReservationStatus.jsx` - компонент
- `src/components/ReservationStatus.css` - стили
- `src/pages/PropertyDetailClassic.jsx` - интеграция (строки 1189-1202)

### 3. Блокировка кнопки "Купить сейчас"

**Для чужой брони**:
```
┌────────────────────────────┐
│  Объект забронирован       │ ← Серая, неактивная
└────────────────────────────┘
```

**Для своей брони или свободного объекта**:
```
┌────────────────────────────┐
│  Купить сейчас             │ ← Активная, зеленая
└────────────────────────────┘
```

**Реализовано в**:
- `src/pages/PropertyDetailClassic.jsx` (строки 1195-1233)

## 🎨 Цветовая схема

### Бейджи на карточках:
- **Фон**: Оранжевый градиент `linear-gradient(135deg, #ffa726 0%, #fb8c00 100%)`
- **Текст**: Белый `#ffffff`
- **Тень**: `0 2px 8px rgba(255, 167, 38, 0.4)`

### Блок на странице объекта (своя бронь):
- **Фон**: Зеленый градиент `linear-gradient(135deg, #0ABAB5 0%, #089a95 100%)`
- **Текст**: Белый `#ffffff`
- **Иконка**: Часы (FiClock) с пульсацией

### Блок на странице объекта (чужая бронь):
- **Фон**: Оранжевый градиент `linear-gradient(135deg, #ffa726 0%, #fb8c00 100%)`
- **Текст**: Белый `#ffffff`
- **Иконка**: Замок (FiLock)

## 🔄 Автоматическое обновление

### Компонент ReservationStatus:
- ✅ Проверяет статус брони при загрузке
- ✅ Обновляет статус каждые **30 секунд**
- ✅ Обновляет таймер каждую **1 секунду**
- ✅ Автоматически исчезает при истечении времени
- ✅ Вызывает callback `onReservationExpired()` для обновления страницы

### Карточки объектов:
- ✅ Бейдж отображается на основе поля `reserved_until` из БД
- ✅ Автоматически скрывается при истечении времени (проверка: `new Date(reserved_until) > new Date()`)

## 📱 Адаптивность

### Мобильные устройства:
- Компонент `ReservationStatus` переключается на вертикальную компоновку
- Бейдж на карточках остается в левом верхнем углу
- Все элементы масштабируются пропорционально

## 🧪 Как проверить визуал

### 1. Перезапустите сервер:
```bash
npm run server
```

### 2. Откройте сайт и отправьте запрос на покупку:
- Выберите любой объект
- Нажмите "Купить сейчас"
- Отправьте запрос

### 3. Проверьте визуал:

#### На главной странице:
- Вернитесь на главную
- Найдите объект, на который отправили запрос
- Должен появиться **оранжевый бейдж "🔒 Забронирован"** в левом верхнем углу

#### На странице объекта:
- Откройте тот же объект
- Над блоком с ценой должен появиться **зеленый блок**:
  ```
  🕐 Вы забронировали этот объект
     Осталось: 71ч 59м
  ```
- Таймер должен обновляться каждую секунду

#### Для другого пользователя:
- Выйдите и войдите под другим аккаунтом
- Откройте тот же объект
- Должен появиться **оранжевый блок**:
  ```
  🔒 Объект забронирован
     Осталось: 71ч 58м
     Другие пользователи не могут отправить запрос на покупку
  ```
- Кнопка "Купить сейчас" должна быть **серой и неактивной**

### 4. Проверьте снятие брони:
- Войдите в админку
- Откройте "Запросы на покупку"
- Нажмите "Отменить" или "Удалить запрос"
- Вернитесь к объекту
- Бейдж и блок брони должны **исчезнуть**
- Кнопка "Купить сейчас" должна стать **активной**

## 📋 Файлы с визуальными изменениями

### Новые файлы:
1. ✅ `src/components/ReservationStatus.jsx` - компонент статуса брони
2. ✅ `src/components/ReservationStatus.css` - стили компонента

### Измененные файлы:
3. ✅ `src/pages/PropertyDetailClassic.jsx` - добавлен компонент и блокировка кнопки
4. ✅ `src/pages/MainPage.jsx` - добавлены бейджи на карточки
5. ✅ `src/pages/MainPage.css` - стили для бейджей

## 🎯 Ключевые особенности

### 1. Разные цвета для разных ситуаций:
- **Зеленый** = Вы забронировали (можете отправить запрос повторно)
- **Оранжевый** = Кто-то другой забронировал (нельзя взаимодействовать)

### 2. Живой таймер:
- Обновляется каждую секунду
- Показывает точное оставшееся время
- Автоматически исчезает при истечении

### 3. Плавные анимации:
- Появление бейджа: `fadeInBadge`
- Пульсация иконки часов: `pulse`
- Переходы при наведении

### 4. Информативность:
- Четко видно, забронирован ли объект
- Понятно, сколько времени осталось
- Ясно, можно ли взаимодействовать с объектом

## 🔧 Кастомизация

### Изменить цвет бейджа на карточках:

В `src/pages/MainPage.css` найдите `.property-reserved-badge`:
```css
.property-reserved-badge {
  background: linear-gradient(135deg, #ВАШ_ЦВЕТ_1 0%, #ВАШ_ЦВЕТ_2 100%);
}
```

### Изменить текст бейджа:

В `src/pages/MainPage.jsx` найдите:
```jsx
<span>Забронирован</span>
```
И замените на свой текст.

### Изменить размер бейджа:

В `src/pages/MainPage.css`:
```css
.property-reserved-badge {
  padding: 8px 14px; /* Увеличьте/уменьшите padding */
  font-size: 13px; /* Измените размер шрифта */
}
```

## ✅ Готово!

Визуальная система бронирования полностью реализована! После перезапуска сервера вы увидите:

- 🔒 **Оранжевые бейджи** на карточках забронированных объектов
- 🕐 **Живой таймер** на странице объекта
- 🚫 **Заблокированную кнопку** для чужих броней
- ✅ **Автоматическое обновление** статуса
