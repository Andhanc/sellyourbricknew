npm# 🚀 Инструкция по запуску проекта

## 📋 Что нужно перед запуском

### 1. Проверьте, установлен Node.js
Убедитесь, что у вас установлен Node.js (рекомендуется версия 18+). Проверьте командой:
```bash
node --version
npm --version
```

## 🔧 Шаги по запуску

### Шаг 1: Установка зависимостей

Если вы еще не устанавливали зависимости, выполните:
```bash
npm install
```

Это установит все необходимые пакеты из `package.json`.

### Шаг 2: Запуск проекта

Проект состоит из двух частей:
- **Фронтенд** (React + Vite) - порт 5173
- **Бэкенд** (Express.js + SQLite) - порт 3000

#### Вариант А: Запуск в режиме разработки (рекомендуется)

Откройте **два терминала**:

**Терминал 1 - Запуск фронтенда:**
```bash
npm run dev
```
Фронтенд будет доступен по адресу: `http://localhost:5173`

**Терминал 2 - Запуск сервера:**
```bash
npm run server
```
Сервер будет доступен по адресу: `http://localhost:3000`

#### Вариант Б: Автоматический режим разработки сервера (с авто-перезагрузкой)
```bash
npm run server:dev
```

## 📦 База данных

### ✅ Автоматическая инициализация

База данных SQLite **создается автоматически** при первом запуске сервера!

- Файл БД будет создан: `server/database.sqlite`
- Все необходимые таблицы создаются автоматически
- Миграции применяются при первом запуске

**Вам НЕ нужно вручную:**
- ❌ Создавать базу данных
- ❌ Запускать SQL-скрипты
- ❌ Настраивать структуру таблиц

### 📂 Структура базы данных

При первом запуске автоматически создаются таблицы:
- `users` - пользователи
- `documents` - документы для верификации
- `notifications` - уведомления
- `administrators` - администраторы

Все индексы и связи создаются автоматически.

## ⚙️ Дополнительные настройки (опционально)

### .env.local файл (ОБЯЗАТЕЛЬНО для Clerk)

**✅ Поддержка обоих форматов переменных окружения!**

Проект поддерживает **оба формата**:
- `REACT_APP_*` (Create React App формат) - **рекомендуется**
- `VITE_*` (Vite формат) - также работает

Создайте файл `.env.local` в корне проекта (рядом с `package.json`):

```env
# WhatsApp (Green API)
VITE_GREEN_API_URL=https://api.green-api.com
VITE_GREEN_API_ID=ваш_id
VITE_GREEN_API_TOKEN=ваш_токен

# EmailJS
VITE_EMAILJS_SERVICE_ID=service_xxxxxxx
VITE_EMAILJS_TEMPLATE_ID=template_xxxxxxx
VITE_EMAILJS_PUBLIC_KEY=ваш_ключ

# Google OAuth
VITE_GOOGLE_CLIENT_ID=ваш_клиент_id

# Clerk (ОБЯЗАТЕЛЬНО для Google/Facebook авторизации)
# Поддерживаются оба формата: REACT_APP_ (Create React App) и VITE_ (Vite)
REACT_APP_CLERK_PUBLISHABLE_KEY=pk_test_bWVldC1ob3VuZC01NC5jbGVyay5hY2NvdW50cy5kZXYk
# Или используйте VITE_ формат (работают оба):
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_bWVldC1ob3VuZC01NC5jbGVyay5hY2NvdW50cy5kZXYk
```

**✅ РЕКОМЕНДУЕТСЯ использовать формат Create React App:**

```env
REACT_APP_CLERK_PUBLISHABLE_KEY=pk_test_bWVldC1ob3VuZC01NC5jbGVyay5hY2NvdW50cy5kZXYk
```

**⚠️ КРИТИЧЕСКИ ВАЖНО:**
- Используйте **`REACT_APP_CLERK_PUBLISHABLE_KEY`** (формат Create React App) или `VITE_CLERK_PUBLISHABLE_KEY` (оба работают)
- После создания/изменения `.env.local` **обязательно перезапустите** `npm run dev`
- Подробная инструкция: см. файл `ENV_SETUP.md`

**Важно:** Без `.env.local` файла с `REACT_APP_CLERK_PUBLISHABLE_KEY` (или `VITE_CLERK_PUBLISHABLE_KEY`) авторизация через Google/Facebook **НЕ будет работать**!

### Что происходит при первом запуске?

1. Сервер запускается на порту 3000
2. База данных `database.sqlite` создается автоматически (если её нет)
3. Выполняются все SQL-миграции
4. Создаются все необходимые таблицы и индексы
5. Проверяется структура БД и при необходимости обновляется

## 🎯 Проверка работы

После запуска:

1. Откройте браузер: `http://localhost:5173`
2. Проверьте, что фронтенд загружается
3. Проверьте консоль сервера - не должно быть ошибок БД

## 🔍 Устранение проблем

### Ошибка "Cannot find module"
Выполните: `npm install`

### Ошибка "Port already in use"
Закройте другие приложения, использующие порты 3000 или 5173, или измените порты в конфигурации.

### Ошибка базы данных
Если возникли проблемы с БД:
- Удалите файл `server/database.sqlite` (если он есть)
- Перезапустите сервер - БД создастся заново
- Все данные будут потеряны!

### WhatsApp не работает
Убедитесь, что:
- Файл `.env` создан и заполнен
- Переменные начинаются с `VITE_`
- Сервер перезапущен после изменения `.env`

## 📝 Полезные команды

```bash
# Установка зависимостей
npm install

# Запуск фронтенда (порт 5173)
npm run dev

# Запуск сервера (порт 3000)
npm run server

# Запуск сервера с авто-перезагрузкой
npm run server:dev

# Сборка для продакшена
npm run build

# Предпросмотр сборки
npm run preview
```

## ✅ Готово!

Проект должен запуститься без дополнительных действий с базой данных. Всё настроится автоматически при первом запуске сервера.

---

**Примечание:** Все данные из базы данных сохраняются в файле `server/database.sqlite`. Для сброса БД просто удалите этот файл и перезапустите сервер.
