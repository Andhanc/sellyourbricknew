# Чеклист переменных окружения для Railway

## ✅ КРИТИЧЕСКИ ВАЖНО - Установите в Railway Dashboard:

### Settings → Variables:

**ОБЯЗАТЕЛЬНО:**
- `SERVER_PORT=3000` ← **ЭТО НУЖНО УСТАНОВИТЬ!**

**Автоматически устанавливается Railway (НЕ добавляйте вручную):**
- `PORT` - Railway установит автоматически (обычно 8080 или другой)

**Опционально (если используете):**
- `REACT_APP_CLERK_PUBLISHABLE_KEY` - если используете Clerk
- `REACT_APP_GOOGLE_CLIENT_ID` - если используете Google OAuth
- `REACT_APP_EMAILJS_*` - если используете EmailJS
- Все остальные переменные вашего приложения

## 📊 Текущее состояние (из логов):

Из ваших логов видно:
- ✅ `PORT: 8080` - установлен Railway (правильно)
- ❌ `SERVER_PORT: не установлен` - **НУЖНО УСТАНОВИТЬ!**

## 🔧 Как установить:

1. Откройте Railway Dashboard
2. Выберите ваш сервис
3. Перейдите в **Settings** → **Variables**
4. Нажмите **+ New Variable**
5. Имя: `SERVER_PORT`
6. Значение: `3000`
7. Нажмите **Add**

## ✅ После установки:

После установки `SERVER_PORT=3000` в логах должно быть:
- `🔧 SERVER_PORT: 3000` (вместо "не установлен, используется 3000")

## 🚨 Почему это важно:

- Сервер использует `SERVER_PORT` для своего порта (3000)
- Vite использует `PORT` для своего порта (8080 от Railway)
- Если `SERVER_PORT` не установлен, сервер все равно будет на 3000, но лучше явно указать
