# Исправление ошибки WhatsApp "Runtime.evaluate timed out"

## ✅ Что было исправлено:

### 1. Увеличен timeout для Puppeteer:
- **Было:** `protocolTimeout: 300000` (5 минут)
- **Стало:** `protocolTimeout: 600000` (10 минут)
- **Причина:** На Railway с ограниченными ресурсами инициализация Chrome/Puppeteer может занимать больше времени

### 2. Улучшена обработка ошибок:
- Добавлена специальная обработка для `timeout` ошибок
- Более информативные сообщения об ошибках
- Четкое указание, что сервер продолжит работу

### 3. Добавлены автоматические повторные попытки:
- **Максимум попыток:** 3
- **Задержка между попытками:** 30 секунд
- **Логирование:** Каждая попытка логируется с номером

## 📋 Как это работает:

1. **Первая попытка:** Сразу при запуске сервера
2. **При timeout ошибке:** Автоматическая повторная попытка через 30 секунд
3. **Максимум 3 попытки:** Если все попытки неудачны, сервер продолжит работу без WhatsApp

## ⚠️ Важно:

- **Сервер продолжает работать** даже если WhatsApp не инициализировался
- **API работает нормально** без WhatsApp функциональности
- **WhatsApp функции** (отправка кодов, уведомлений) будут недоступны до успешной инициализации

## 🔍 Что делать, если WhatsApp не инициализируется:

1. **Проверьте логи** через несколько минут после деплоя
2. **Перезапустите сервис** на Railway (может помочь)
3. **Проверьте ресурсы** Railway (возможно, не хватает памяти/CPU)
4. **Проверьте системные библиотеки** - все должны быть установлены в Dockerfile

## 📊 Ожидаемое поведение:

### Успешная инициализация:
```
🔄 Попытка инициализации WhatsApp (1/3)...
✅ WhatsApp клиент готов к отправке сообщений
```

### Timeout (нормально на Railway):
```
🔄 Попытка инициализации WhatsApp (1/3)...
❌ Ошибка при инициализации WhatsApp клиента: Runtime.evaluate timed out
⚠️ Таймаут при инициализации WhatsApp (это нормально на Railway).
🔄 Повторная попытка инициализации через 30 секунд...
```

### После 3 неудачных попыток:
```
⚠️ Достигнут лимит попыток инициализации.
✅ Сервер продолжает работу без WhatsApp функциональности.
```

## 🎯 Итого:

- ✅ Timeout увеличен до 10 минут
- ✅ Автоматические повторные попытки (3 раза)
- ✅ Сервер не падает при ошибках WhatsApp
- ✅ Информативные логи для диагностики
