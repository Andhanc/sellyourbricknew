# 🔧 Исправление настроек Paths в Clerk Dashboard

## Проблема
В настройках Paths не указан "Fallback development host", что может мешать Clerk перенаправлять на localhost после OAuth callback.

## ✅ Что нужно сделать

### 1. Настройте Fallback development host

1. В Clerk Dashboard на странице **Paths** (которую вы сейчас видите)
2. Найдите раздел **"Fallback development host"**
3. В поле ввода добавьте (попробуйте разные варианты, если один не работает):
   - Вариант 1: `http://localhost:5173` (без слеша в конце)
   - Вариант 2: `http://localhost:5173/` (со слешем в конце)
   - Вариант 3: `http://127.0.0.1:5173` (с IP вместо localhost)
   
4. **Если появляется ошибка валидации:**
   - Убедитесь, что URL начинается с `http://` или `https://`
   - Убедитесь, что нет лишних пробелов
   - Попробуйте удалить и ввести заново
   - Попробуйте другой вариант (с IP или со слешем)

5. После того, как ошибка исчезнет, нажмите **"Save"** (фиолетовая кнопка внизу)

### 2. Проверьте Component paths (опционально)

Раздел **"Component paths"** можно оставить как есть (на Account Portal), так как вы используете кастомные кнопки для входа через Google/Facebook.

Но если хотите, можете изменить на "development host" для SignIn/SignUp - это не критично для OAuth.

### 3. Проверьте Application paths

1. Раздел **"Application paths"** → **"Home URL"**
   - Можно оставить пустым или указать `/`
   
2. Раздел **"Unauthorized sign in URL"**
   - Можно оставить пустым

### 4. Сохраните изменения

После настройки **Fallback development host** нажмите кнопку сохранения внизу страницы.

### 5. Попробуйте снова

1. Подождите 1-2 минуты после сохранения
2. Очистите кеш браузера
3. Попробуйте войти через Google снова

## ⚠️ Важно

**Fallback development host** должен быть:
```
http://localhost:5173
```

**БЕЗ слеша** в конце и **БЕЗ пути** `/profile` - только домен и порт!

## 📝 Итоговые настройки

- **Fallback development host**: `http://localhost:5173`
- **Home URL**: можно оставить пустым или `/`
- **Component paths**: можно оставить на Account Portal (не критично)

После этого Clerk должен правильно перенаправлять на localhost после OAuth callback.

