# Исправление ошибки "Configure network" на Railway

## Проблема:
Railway не может настроить сеть при деплое. Ошибка возникает на этапе "Deploy > Configure network".

## Решение:

### 1. В Railway Dashboard:
- Перейдите в **Settings** → **Networking**
- Убедитесь, что сервис **экспонирован** (Generate Domain включен)
- Или создайте публичный домен для сервиса

### 2. Переменные окружения в Railway:
Убедитесь, что установлены:
- `SERVER_PORT=3000` - порт для бэкенд-сервера
- `PORT` - Railway установит автоматически для Vite (основной веб-сервис)

### 3. Проверка портов:
- **Vite (фронтенд)**: должен слушать на `PORT` (Railway установит автоматически)
- **Server (бэкенд)**: должен слушать на `SERVER_PORT` (3000)
- Оба должны слушать на `0.0.0.0` (все интерфейсы)

### 4. Что было исправлено в коде:
- ✅ Vite теперь слушает на `0.0.0.0` вместо `true`
- ✅ Сервер теперь слушает на `0.0.0.0` вместо localhost
- ✅ Добавлен `strictPort: false` в vite.config.js

### 5. Если проблема сохраняется:
1. Проверьте логи в Railway Dashboard → View logs
2. Убедитесь, что оба процесса запускаются:
   - SERVER должен показать: "🚀 Сервер запущен на порту 3000"
   - FRONTEND должен показать: "VITE v5.x.x ready in xxx ms"
3. Проверьте, что сервис экспонирован в настройках Railway
