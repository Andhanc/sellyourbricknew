# 🔍 Диагностика проблемы OAuth (данные не подтягиваются)

## Проблема
После авторизации через Google/Facebook пользователь не определяется (`isSignedIn: false`), данные не подтягиваются.

## ✅ Пошаговая проверка настроек

### 1. Проверка Redirect URIs в Google Cloud Console

1. Откройте: https://console.cloud.google.com/apis/credentials
2. Найдите ваш **OAuth 2.0 Client ID** (который вы создали)
3. Нажмите на него для редактирования
4. Проверьте раздел **"Authorized redirect URIs"**

**ДОЛЖНО БЫТЬ:**
```
https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback
```

**НЕ ДОЛЖНО БЫТЬ:**
- `http://localhost:5173`
- `http://localhost:5173/profile`
- `http://localhost:5173/callback`
- Любые другие URI с localhost или вашим доменом

5. Если URI неправильный:
   - Удалите неправильные URI
   - Добавьте правильный: `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
   - Нажмите **"SAVE"**

6. **ВАЖНО:** Подождите 2-3 минуты после сохранения (настройки применяются с задержкой)

### 2. Проверка Authorized JavaScript origins в Google Cloud Console

В том же окне редактирования OAuth Client ID проверьте раздел **"Authorized JavaScript origins"**:

**ДОЛЖНО БЫТЬ:**
```
https://meet-hound-54.clerk.accounts.dev
```

**БЕЗ пути** `/v1/oauth_callback` и **БЕЗ слеша** в конце!

### 3. Проверка настроек в Clerk Dashboard

1. Откройте: https://dashboard.clerk.com/
2. Выберите ваше приложение
3. Перейдите: **User & Authentication** → **Social Connections** → **Google**
4. Проверьте:
   - ✅ **"Use custom credentials"** включен (фиолетовый)
   - ✅ **Client ID** вставлен правильно
   - ✅ **Client Secret** вставлен правильно
   - ✅ **"Authorized Redirect URI"** должен быть: `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
   - ✅ **"Enable for sign-up and sign-in"** включен (фиолетовый)
5. Нажмите **"Update"** если что-то изменили

### 4. Проверка домена в Clerk Dashboard

1. В Clerk Dashboard перейдите: **Settings** → **Domains**
2. Убедитесь, что ваш домен разработки: `meet-hound-54.clerk.accounts.dev`
3. Проверьте, что домен активен

### 5. Проверка OAuth Consent Screen в Google

1. Откройте: https://console.cloud.google.com/apis/credentials/consent
2. Убедитесь, что OAuth consent screen настроен:
   - App name заполнен
   - User support email указан
   - Developer contact information указан
3. Если приложение в режиме "Testing":
   - Добавьте ваш email в раздел **"Test users"** (если нужно)

## 🔧 Что делать после проверки

1. **Очистите кеш браузера:**
   - Нажмите `Ctrl+Shift+Delete`
   - Выберите "Cookies" и "Cached images and files"
   - Очистите для `localhost` и `clerk.accounts.dev`

2. **Очистите sessionStorage:**
   - Откройте консоль браузера (F12)
   - Выполните: `sessionStorage.clear()`

3. **Перезапустите dev-сервер:**
   ```bash
   npm run dev
   ```

4. **Попробуйте войти снова:**
   - Откройте приложение
   - Попробуйте войти через Google
   - Откройте консоль браузера (F12) и следите за логами

## 📊 Что смотреть в консоли браузера

После попытки входа через Google проверьте консоль:

### ✅ Хорошие признаки:
- `ClerkAuthHandler: User authenticated, saving data`
- `isSignedIn: true`
- `hasUser: true`
- Появляется объект `user` с данными

### ❌ Плохие признаки:
- `ClerkAuthHandler: Timeout waiting for user data`
- `isSignedIn: false`
- `hasUser: false`
- Нет OAuth параметров в URL после редиректа

## 🆘 Если все равно не работает

### Проверьте URL после редиректа:

1. После нажатия "Войти через Google" вы должны быть перенаправлены на Google
2. После авторизации в Google вы должны вернуться на Clerk домен
3. Затем Clerk должен перенаправить вас на ваш сайт

**Проверьте URL в адресной строке после каждого шага:**
- Шаг 1: `https://accounts.google.com/...` (авторизация в Google)
- Шаг 2: `https://meet-hound-54.clerk.accounts.dev/...` (обработка Clerk)
- Шаг 3: `http://localhost:5173/profile` (ваш сайт)

Если на шаге 2 вы не попадаете на Clerk домен, значит Redirect URI в Google настроен неправильно.

### Создайте новый OAuth Client ID (если ничего не помогает):

1. В Google Cloud Console создайте новый OAuth 2.0 Client ID
2. Добавьте правильные Redirect URIs с самого начала
3. Скопируйте новый Client ID и Client Secret
4. Обновите их в Clerk Dashboard

## 📝 Чеклист для быстрой проверки

- [ ] Redirect URI в Google: `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
- [ ] JavaScript origin в Google: `https://meet-hound-54.clerk.accounts.dev` (без пути)
- [ ] Client ID и Secret вставлены в Clerk Dashboard
- [ ] "Use custom credentials" включен в Clerk
- [ ] "Enable for sign-up and sign-in" включен в Clerk
- [ ] OAuth Consent Screen настроен в Google
- [ ] Кеш браузера очищен
- [ ] Dev-сервер перезапущен


