# 🔧 Проверка настроек доменов в Clerk Dashboard

## Проблема
`redirect_uri` в Google правильный (`https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`), но после редиректа Clerk не определяет пользователя (`isSignedIn: false`).

## ✅ Проверка настроек в Clerk Dashboard

### 1. Проверка доменов

1. Откройте: https://dashboard.clerk.com/
2. Выберите ваше приложение
3. Перейдите: **Settings** → **Domains** (или **Paths** → **Domains**)
4. Проверьте:
   - ✅ Должен быть домен разработки: `meet-hound-54.clerk.accounts.dev`
   - ✅ Для разработки должен быть разрешен `localhost:5173` (или автоматически разрешен)

### 2. Проверка социальных подключений

1. Перейдите: **User & Authentication** → **Social Connections** → **Google**
2. Проверьте:
   - ✅ **"Use custom credentials"** включен (фиолетовый)
   - ✅ **Client ID** правильный: `1028494020168-034f525smlhane9lc4f041bbugokjt1i.apps.googleusercontent.com`
   - ✅ **Client Secret** правильный
   - ✅ **"Authorized Redirect URI"** должен быть: `https://meet-hound-54.clerk.accounts.dev/v1/oauth_callback`
   - ✅ **"Enable for sign-up and sign-in"** включен (фиолетовый)

### 3. Проверка путей (Paths)

1. Перейдите: **Paths** (или **Settings** → **Paths**)
2. Проверьте настройки редиректов:
   - **After sign-in URL**: `/profile` или `http://localhost:5173/profile`
   - **After sign-up URL**: `/profile` или `http://localhost:5173/profile`

### 4. Проверка настроек приложения

1. Перейдите: **Settings** → **General**
2. Проверьте:
   - **Allowed origins** - должен быть `http://localhost:5173` (или автоматически разрешен для разработки)
   - **Allowed redirect URLs** - должен быть `http://localhost:5173/profile` (или автоматически разрешен)

## 🔍 Диагностика через Network tab

После нажатия "Войти через Google" и выбора аккаунта:

1. Откройте DevTools (F12) → **Network**
2. Найдите запросы к `clerk.accounts.dev` или `clerk.com`
3. Проверьте:
   - Есть ли запросы к Clerk после редиректа от Google?
   - Какие статус-коды у этих запросов (200, 302, 400, 500)?
   - Есть ли ошибки (красные запросы)?

## ⚠️ Возможные проблемы

1. **Clerk не может перенаправить на localhost** - проверьте настройки доменов
2. **OAuth callback не обрабатывается** - проверьте логи в Network tab
3. **Проблема с сессией** - может быть проблема с cookies или CORS

## 📝 Что проверить в Network tab

После выбора аккаунта Google проверьте:

1. **Редирект на Clerk домен:**
   - Должен быть запрос к `https://meet-hound-54.clerk.accounts.dev/...`
   - Статус должен быть 200 или 302

2. **Редирект обратно на localhost:**
   - Должен быть запрос к `http://localhost:5173/profile`
   - Проверьте, есть ли параметры в URL (например, `?__clerk_...`)

3. **Ошибки:**
   - Если есть запросы с ошибками (400, 500), откройте их и проверьте Response

## 🆘 Если все равно не работает

Попробуйте:
1. В Clerk Dashboard → **Settings** → **Domains** → добавьте явно `http://localhost:5173`
2. Проверьте, нет ли блокировок CORS в консоли браузера
3. Проверьте cookies - должны быть cookies от `clerk.accounts.dev`


