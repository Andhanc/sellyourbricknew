# Подробное руководство по авторизации через WhatsApp

## 📱 Что такое Green API?

**Green API** — это сервис, который позволяет отправлять и получать сообщения через WhatsApp через API (программный интерфейс). 

### Как это работает:

1. **Вы регистрируетесь в Green API** и создаете "инстанс" (экземпляр подключения)
2. **Подключаете свой WhatsApp аккаунт** к этому инстансу через QR-код
3. **Получаете API ключи** (`idInstance` и `apiTokenInstance`)
4. **Используете эти ключи** для отправки сообщений через API

### Это не бот в привычном понимании!

- ❌ Это **НЕ** отдельный бот-аккаунт в WhatsApp
- ✅ Это **ваш личный WhatsApp аккаунт**, подключенный к API
- ✅ Вы можете отправлять сообщения от имени этого аккаунта
- ✅ Сообщения приходят пользователям как обычные WhatsApp сообщения

### Аналогия:

Представьте, что Green API — это "пульт управления" для вашего WhatsApp. Вы подключаете свой телефон к этому пульту, и теперь можете отправлять сообщения программно (через код), а не только вручную через приложение.

---

## 🔄 Полный процесс авторизации через WhatsApp

### Шаг 1: Пользователь нажимает "Войти через WhatsApp"

```
Пользователь → Нажимает кнопку → Открывается модальное окно
```

**Что происходит в коде:**
- В `LoginModal.jsx` функция `handleWhatsAppLogin()` открывает `WhatsAppVerificationModal`
- Показывается форма для ввода номера телефона

---

### Шаг 2: Пользователь вводит номер телефона

```
Пользователь → Вводит номер → Нажимает "Отправить код"
```

**Что происходит в коде:**

1. **Форматирование номера** (`authService.js` → `formatPhoneNumber()`):
   ```javascript
   // Пример: пользователь ввел "8 999 123-45-67"
   // Преобразуется в: "79991234567"
   ```

2. **Генерация кода** (`authService.js` → `generateVerificationCode()`):
   ```javascript
   // Генерируется случайный 6-значный код
   // Пример: "123456"
   ```

3. **Сохранение кода** (временно в `localStorage`):
   ```javascript
   // Код сохраняется с номером телефона и временем истечения (10 минут)
   {
     "79991234567": {
       code: "123456",
       timestamp: 1234567890,
       expiresAt: 1234568490  // +10 минут
     }
   }
   ```

---

### Шаг 3: Отправка кода через Green API

```
Ваш сайт → Green API → WhatsApp → Пользователь получает сообщение
```

**Что происходит в коде** (`authService.js` → `sendWhatsAppVerificationCode()`):

#### Вариант A: Green API настроен ✅

```javascript
// 1. Проверяем, есть ли API ключи
if (GREEN_API_ID && GREEN_API_TOKEN) {
  
  // 2. Отправляем запрос в Green API
  fetch(`https://api.green-api.com/waInstance${GREEN_API_ID}/sendMessage/${GREEN_API_TOKEN}`, {
    method: 'POST',
    body: JSON.stringify({
      chatId: "79991234567@c.us",  // Формат: номер@c.us
      message: "🔐 Ваш код авторизации: 123456\n\nКод действителен в течение 10 минут."
    })
  })
  
  // 3. Green API отправляет сообщение в WhatsApp
  // 4. Пользователь получает сообщение в своем WhatsApp
}
```

**Формат номера для Green API:**
- Номер должен быть в формате: `79991234567@c.us`
- `@c.us` означает "chat" (личный чат)
- Для групп используется `@g.us`

#### Вариант B: Green API не настроен (демо режим) ⚠️

```javascript
// Если API не настроен, открывается WhatsApp с предзаполненным сообщением
const whatsappUrl = `https://wa.me/79991234567?text=Ваш код: 123456`
window.open(whatsappUrl, '_blank')

// Пользователь должен скопировать код из открывшегося чата
// В production это НЕ должно использоваться!
```

---

### Шаг 4: Пользователь получает код в WhatsApp

```
Green API → WhatsApp сервер → Телефон пользователя → Сообщение в WhatsApp
```

**Что видит пользователь:**
```
🔐 Ваш код авторизации: 123456

Код действителен в течение 10 минут.

Если вы не запрашивали этот код, проигнорируйте это сообщение.
```

---

### Шаг 5: Пользователь вводит код на сайте

```
Пользователь → Видит код в WhatsApp → Вводит код на сайте → Нажимает "Проверить"
```

**Что происходит в коде** (`WhatsAppVerificationModal.jsx`):

1. **Ввод кода**:
   - 6 отдельных полей для ввода
   - Автоматический переход между полями
   - Поддержка вставки (paste) полного кода

2. **Автоматическая проверка**:
   - Когда все 6 полей заполнены, код автоматически проверяется

---

### Шаг 6: Проверка кода и авторизация

```
Сайт → Проверяет код → Создает/находит пользователя → Авторизует
```

**Что происходит в коде** (`authService.js` → `verifyWhatsAppCode()`):

1. **Проверка кода**:
   ```javascript
   // Получаем сохраненный код из localStorage
   const storedCode = localStorage.getItem('whatsappCodes')
   
   // Проверяем:
   // - Существует ли код для этого номера?
   // - Совпадает ли введенный код с сохраненным?
   // - Не истек ли код? (10 минут)
   ```

2. **Если код верный**:
   ```javascript
   // Создаем данные пользователя
   const userData = {
     phone: "79991234567",
     name: "Пользователь 4567",  // Последние 4 цифры номера
     id: "whatsapp_79991234567",
     role: "client"
   }
   
   // Сохраняем в localStorage
   saveUserData(userData, 'whatsapp')
   ```

3. **Если код неверный**:
   ```javascript
   return {
     success: false,
     error: "Неверный код" или "Код истек"
   }
   ```

---

### Шаг 7: Успешная авторизация

```
Код верный → Пользователь авторизован → Перенаправление на профиль
```

**Что происходит:**
- Данные пользователя сохраняются в `localStorage`
- Модальное окно закрывается
- Показывается уведомление: "Добро пожаловать, Пользователь 4567!"
- Перенаправление на страницу `/profile`

---

## 🛠️ Пошаговая настройка Green API

### Шаг 1: Регистрация

1. Перейдите на https://green-api.com/
2. Нажмите "Регистрация" или "Sign Up"
3. Заполните форму:
   - Email
   - Пароль
   - Подтвердите email

### Шаг 2: Создание инстанса

1. После входа в личный кабинет нажмите "Создать инстанс" или "Create instance"
2. Выберите тариф:
   - **Бесплатный**: до 100 сообщений в день
   - **Платный**: больше сообщений и функций
3. Запишите или скопируйте:
   - `idInstance` (например: `1101234567`)
   - `apiTokenInstance` (длинная строка символов)

### Шаг 3: Подключение WhatsApp аккаунта

1. В личном кабинете найдите раздел "QR-код" или "QR Code"
2. Откройте WhatsApp на телефоне
3. Перейдите: **Настройки → Связанные устройства → Связать устройство**
4. Отсканируйте QR-код из личного кабинета Green API
5. Дождитесь подтверждения подключения

**Важно:**
- Используйте тот же номер телефона, который указан в Green API
- QR-код действителен ограниченное время (обычно 1-2 минуты)
- Если QR-код истек, обновите страницу и получите новый

### Шаг 4: Настройка переменных окружения

1. В корне проекта создайте файл `.env` (если его нет)
2. Добавьте следующие строки:

```env
# Green API настройки
VITE_GREEN_API_URL=https://api.green-api.com
VITE_GREEN_API_ID=1101234567
VITE_GREEN_API_TOKEN=ваш_длинный_токен_здесь
```

**Пример:**
```env
VITE_GREEN_API_URL=https://api.green-api.com
VITE_GREEN_API_ID=1101234567
VITE_GREEN_API_TOKEN=abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
```

3. Сохраните файл
4. **Перезапустите dev сервер** (`npm run dev`)

**Почему нужен перезапуск?**
- Vite загружает переменные окружения только при старте
- Изменения в `.env` не применяются без перезапуска

### Шаг 5: Проверка работы

1. Откройте сайт: `http://localhost:5173`
2. Нажмите "Войти" или "Регистрация"
3. Нажмите "Войти через WhatsApp"
4. Введите тестовый номер телефона (ваш номер для проверки)
5. Нажмите "Отправить код"
6. Проверьте WhatsApp — должно прийти сообщение с кодом
7. Введите код на сайте
8. Должна произойти авторизация

---

## 📋 Структура файлов и их роль

### 1. `src/services/authService.js`

**Роль:** Основная логика авторизации

**Функции:**
- `sendWhatsAppVerificationCode(phone)` — отправка кода
- `verifyWhatsAppCode(phone, code)` — проверка кода
- `formatPhoneNumber(phone)` — форматирование номера
- `generateVerificationCode()` — генерация 6-значного кода
- `saveVerificationCode(phone, code)` — сохранение кода (временное)
- `verifyCode(phone, code)` — проверка сохраненного кода

### 2. `src/components/WhatsAppVerificationModal.jsx`

**Роль:** UI для ввода номера и кода

**Что делает:**
- Показывает форму ввода номера телефона
- Показывает 6 полей для ввода кода
- Обрабатывает отправку кода
- Обрабатывает проверку кода
- Показывает ошибки и таймер

### 3. `src/components/LoginModal.jsx`

**Роль:** Главное модальное окно входа/регистрации

**Что делает:**
- Показывает кнопку "Войти через WhatsApp"
- Открывает `WhatsAppVerificationModal` при клике
- Обрабатывает успешную авторизацию

---

## 🔐 Безопасность и ограничения

### Текущая реализация (для разработки):

⚠️ **Коды хранятся в localStorage** — это НЕ безопасно для production!

**Проблемы:**
- Коды видны в DevTools
- Коды можно подделать
- Нет защиты от брутфорса
- Нет rate limiting

### Для production нужно:

1. **Backend сервер:**
   - Генерация кодов на сервере
   - Хранение в базе данных
   - Проверка на сервере

2. **Ограничения:**
   - Максимум 3 попытки ввода кода
   - Rate limiting (не более 1 запроса в минуту)
   - Валидация номеров телефонов

3. **HTTPS:**
   - Все запросы должны идти через HTTPS
   - Защита от перехвата данных

---

## 🎯 Лимиты Green API

### Бесплатный тариф:

- ✅ До 100 сообщений в день
- ✅ Базовые функции API
- ✅ Поддержка личных чатов
- ❌ Нет поддержки групп
- ❌ Нет вебхуков (автоматические ответы)

### Платные тарифы:

- 💰 Больше сообщений
- 💰 Вебхуки (автоматические ответы)
- 💰 Поддержка групп
- 💰 Приоритетная поддержка

---

## 🐛 Решение проблем

### Проблема: Код не приходит в WhatsApp

**Возможные причины:**

1. **Green API не подключен к WhatsApp:**
   - Решение: Проверьте QR-код в личном кабинете, переподключите

2. **Неправильный формат номера:**
   - Решение: Номер должен быть в формате `79991234567` (без +, пробелов, скобок)

3. **Лимит сообщений исчерпан:**
   - Решение: Подождите до следующего дня или перейдите на платный тариф

4. **Неправильные API ключи:**
   - Решение: Проверьте `VITE_GREEN_API_ID` и `VITE_GREEN_API_TOKEN` в `.env`

### Проблема: Ошибка "Неверный код"

**Возможные причины:**

1. **Код истек (10 минут):**
   - Решение: Запросите новый код

2. **Код введен неправильно:**
   - Решение: Проверьте, что введены все 6 цифр

3. **Код не сохранен:**
   - Решение: Проверьте консоль браузера на ошибки

### Проблема: Белый экран после ввода кода

**Возможные причины:**

1. **Ошибка в коде:**
   - Решение: Откройте консоль браузера (F12) и проверьте ошибки

2. **Проблема с localStorage:**
   - Решение: Очистите localStorage и попробуйте снова

---

## 📊 Схема работы (визуально)

```
┌─────────────┐
│  Пользователь │
└──────┬───────┘
       │
       │ 1. Нажимает "Войти через WhatsApp"
       ▼
┌─────────────────────┐
│  LoginModal.jsx     │
│  (открывает модалку) │
└──────┬──────────────┘
       │
       │ 2. Показывает форму
       ▼
┌──────────────────────────┐
│ WhatsAppVerificationModal│
│  (ввод номера телефона)  │
└──────┬───────────────────┘
       │
       │ 3. Отправляет номер
       ▼
┌─────────────────────┐
│  authService.js     │
│  sendWhatsApp...()  │
└──────┬──────────────┘
       │
       │ 4. Генерирует код
       │    Сохраняет в localStorage
       │    Отправляет через Green API
       ▼
┌─────────────────────┐
│   Green API         │
│   (API сервер)      │
└──────┬──────────────┘
       │
       │ 5. Отправляет сообщение
       ▼
┌─────────────────────┐
│   WhatsApp          │
│   (серверы Meta)    │
└──────┬──────────────┘
       │
       │ 6. Доставляет сообщение
       ▼
┌─────────────┐
│  Пользователь │
│  (получает код)│
└──────┬───────┘
       │
       │ 7. Вводит код на сайте
       ▼
┌──────────────────────────┐
│ WhatsAppVerificationModal│
│  (ввод кода)             │
└──────┬───────────────────┘
       │
       │ 8. Отправляет код для проверки
       ▼
┌─────────────────────┐
│  authService.js     │
│  verifyWhatsApp...()│
└──────┬──────────────┘
       │
       │ 9. Проверяет код
       │    Создает пользователя
       │    Сохраняет в localStorage
       ▼
┌─────────────────────┐
│  Успешная           │
│  авторизация!        │
│  → /profile          │
└─────────────────────┘
```

---

## 💡 Советы и рекомендации

1. **Для тестирования:**
   - Используйте свой номер телефона
   - Проверьте, что Green API подключен
   - Проверьте лимиты бесплатного тарифа

2. **Для production:**
   - Обязательно создайте backend сервер
   - Не храните коды в localStorage
   - Используйте базу данных
   - Добавьте rate limiting
   - Настройте HTTPS

3. **Альтернативы Green API:**
   - **Whapi** — больше лимитов, но сложнее настройка
   - **Twilio** — платный, но очень надежный
   - **Собственный сервер** — полный контроль, но требует настройки

---

## 📝 Резюме

**Green API** — это сервис, который позволяет отправлять WhatsApp сообщения через API, используя ваш личный WhatsApp аккаунт.

**Процесс авторизации:**
1. Пользователь вводит номер → 
2. Генерируется код → 
3. Код отправляется через Green API → 
4. Пользователь получает код в WhatsApp → 
5. Пользователь вводит код на сайте → 
6. Код проверяется → 
7. Пользователь авторизован ✅

**Для начала работы:**
1. Зарегистрируйтесь в Green API
2. Создайте инстанс
3. Подключите WhatsApp через QR-код
4. Добавьте ключи в `.env`
5. Перезапустите сервер
6. Готово! 🎉

