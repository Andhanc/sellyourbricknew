# Система бронирования объектов недвижимости на 72 часа

## ✅ Что было реализовано

Добавлена полная система автоматического бронирования объектов недвижимости при отправке запроса на покупку.

## 📋 Функционал

### Как это работает:

1. **Пользователь отправляет запрос на покупку** через модальное окно "Купить сейчас"
2. **Объект автоматически бронируется на 72 часа**
3. **Другие пользователи не могут отправить запрос** на этот объект
4. **Таймер отсчитывает оставшееся время** бронирования
5. **Админ может снять бронь** через кнопки "Отменить" или "Удалить запрос" в админке

### Снятие брони происходит автоматически:

- ✅ Когда истекают 72 часа
- ✅ Когда админ нажимает "Отменить" (меняет статус на `cancelled`)
- ✅ Когда админ нажимает "Завершить" (меняет статус на `completed`)
- ✅ Когда админ удаляет запрос на покупку

## 🔧 Технические детали

### 1. База данных

**Файл**: `server/database/add_reservation_fields.sql`

Добавлены поля в таблицы `properties_apartments` и `properties_houses`:
- `reserved_until` (DATETIME) - дата и время окончания брони
- `reserved_by` (INTEGER) - ID пользователя, забронировавшего объект
- `purchase_request_id` (INTEGER) - ID связанного запроса на покупку

**Автоматическая миграция**: При запуске сервера поля добавляются автоматически, если их нет.

### 2. Серверная логика

**Файл**: `server/database/database.js`

Добавлены функции в `apartmentQueries` и `houseQueries`:

```javascript
// Забронировать объект на 72 часа
reserve(id, userId, purchaseRequestId)

// Снять бронь
unreserve(id)

// Проверить статус брони
isReserved(id) // Возвращает { isReserved, reservedUntil, reservedBy, purchaseRequestId, timeRemaining }
```

**Файл**: `server/server.js`

#### Бронирование при создании запроса:
- **Endpoint**: `POST /api/purchase-requests`
- **Логика**: После создания запроса автоматически вызывается `reserve()` для объекта
- **Строки**: 1680-1717

#### Снятие брони при отмене:
- **Endpoint**: `PUT /api/purchase-requests/:id/status`
- **Логика**: При изменении статуса на `cancelled` или `completed` вызывается `unreserve()`
- **Строки**: 1844-1896

#### Снятие брони при удалении:
- **Endpoint**: `DELETE /api/purchase-requests/:id`
- **Логика**: Перед удалением запроса вызывается `unreserve()`
- **Строки**: 1901-1941

#### Получение информации о брони:
- **Endpoint**: `GET /api/properties/:type/:id/reservation`
- **Возвращает**: Информацию о бронировании объекта
- **Строки**: 1943-1972

### 3. Frontend компоненты

**Файл**: `src/components/ReservationStatus.jsx`

Компонент для отображения статуса бронирования:
- Показывает таймер обратного отсчета
- Разные стили для своей и чужой брони
- Автоматически обновляется каждые 30 секунд
- Обновляет таймер каждую секунду

**Файл**: `src/components/ReservationStatus.css`

Стили компонента:
- Зеленый фон для своей брони
- Оранжевый фон для чужой брони
- Анимация появления
- Пульсирующая иконка часов
- Адаптивный дизайн

## 🎨 Использование в UI

### Добавление компонента на страницу объекта:

```jsx
import ReservationStatus from '../components/ReservationStatus';
import { useUser } from '@clerk/clerk-react';
import { getUserData } from '../services/authService';

// В компоненте:
const { user } = useUser();
const userData = getUserData();
const currentUserId = user?.id || userData?.id;

// В JSX (перед кнопкой "Купить сейчас"):
<ReservationStatus
  propertyId={property.id}
  propertyType={property.property_type}
  currentUserId={currentUserId}
  onReservationExpired={() => {
    // Обновить данные объекта
    window.location.reload();
  }}
/>
```

### Блокировка кнопки "Купить сейчас":

```jsx
const [isReserved, setIsReserved] = useState(false);
const [isOwnReservation, setIsOwnReservation] = useState(false);

// Проверка брони при загрузке
useEffect(() => {
  const checkReservation = async () => {
    const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'http://localhost:3000/api';
    const response = await fetch(
      `${API_BASE_URL}/properties/${property.property_type}/${property.id}/reservation`
    );
    
    if (response.ok) {
      const data = await response.json();
      if (data.isReserved) {
        setIsReserved(true);
        setIsOwnReservation(
          currentUserId && String(data.reservedBy) === String(currentUserId)
        );
      }
    }
  };
  
  checkReservation();
}, [property.id, property.property_type, currentUserId]);

// В JSX:
<button
  className="buy-now-btn"
  onClick={handleBookNow}
  disabled={isReserved && !isOwnReservation}
>
  {isReserved && !isOwnReservation ? 'Объект забронирован' : 'Купить сейчас'}
</button>
```

## 🛠️ Админка

### Кнопки для снятия брони

В компоненте `src/components/admin/PurchaseRequests.jsx` уже есть кнопки:

1. **"Отменить"** (строка 1036-1043) - меняет статус на `cancelled`, автоматически снимает бронь
2. **"Завершить"** (строка 1028-1035) - меняет статус на `completed`, автоматически снимает бронь
3. **"Удалить запрос"** (строка 1045-1055) - удаляет запрос, автоматически снимает бронь

**Никаких дополнительных изменений в админке не требуется!**

## 🧪 Тестирование

### 1. Перезапустите сервер:

```bash
npm run server
```

Остановите текущий сервер (Ctrl+C) и запустите заново.

### 2. Проверьте логи при запуске:

Должны увидеть:
```
🔄 Обновление схемы БД: добавляем поля бронирования...
✅ Поля бронирования добавлены в таблицы недвижимости
```

### 3. Отправьте запрос на покупку:

- Откройте любой объект недвижимости
- Нажмите "Купить сейчас"
- Отправьте запрос

### 4. Проверьте логи сервера:

Должны увидеть:
```
✅ Создан новый запрос на покупку: { id: X, ... }
🔒 Объект забронирован на 72 часа: { propertyId: X, ... }
📱 WhatsApp сообщение отправлено покупателю: ...
```

### 5. Проверьте UI:

- Компонент `ReservationStatus` должен появиться на странице объекта
- Таймер должен отсчитывать время
- Кнопка "Купить сейчас" должна быть заблокирована для других пользователей

### 6. Проверьте админку:

- Откройте "Запросы на покупку"
- Нажмите "Отменить" или "Удалить запрос"
- Проверьте логи - должно быть: `🔓 Бронь снята с объекта`

## 📊 API Endpoints

### Получить информацию о бронировании:

```
GET /api/properties/:type/:id/reservation
```

**Параметры**:
- `type` - тип объекта (`apartment`, `house`, `villa`, `commercial`)
- `id` - ID объекта

**Ответ**:
```json
{
  "success": true,
  "isReserved": true,
  "reservedUntil": "2026-02-15T18:49:03.410Z",
  "reservedBy": 36,
  "purchaseRequestId": 6,
  "timeRemaining": 259200000
}
```

## ⚙️ Настройки

### Изменить время бронирования:

В `server/database/database.js` найдите функции `reserve()` и измените:

```javascript
// Вместо 72 часов (72 * 60 * 60 * 1000)
const reservedUntil = new Date(Date.now() + 48 * 60 * 60 * 1000); // 48 часов
```

## 🐛 Возможные проблемы

### Бронь не снимается автоматически:

- Проверьте, что сервер запущен заново после добавления изменений
- Проверьте логи сервера на наличие ошибок

### Компонент ReservationStatus не отображается:

- Убедитесь, что импортирован правильно
- Проверьте, что передаются все необходимые пропсы
- Откройте консоль браузера на наличие ошибок

### Кнопка "Купить сейчас" не блокируется:

- Добавьте логику проверки брони (см. раздел "Использование в UI")
- Проверьте, что API endpoint `/reservation` работает

## ✅ Готово!

Система бронирования полностью реализована и готова к использованию! 

После перезапуска сервера:
- ✅ Объекты бронируются автоматически на 72 часа
- ✅ Таймер отсчитывает оставшееся время
- ✅ Админ может снять бронь через существующие кнопки
- ✅ Бронь снимается автоматически после истечения времени
