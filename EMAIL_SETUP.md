# 📧 Настройка отправки email через EmailJS

Этот документ описывает, как настроить отправку кодов подтверждения на email через EmailJS.

## Шаг 1: Регистрация в EmailJS

1. Перейдите на [https://www.emailjs.com/](https://www.emailjs.com/)
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в Dashboard

## Шаг 2: Настройка Email Service

1. В Dashboard перейдите в раздел **Email Services**
2. Нажмите **Add New Service**
3. Выберите провайдера email (Gmail, Outlook, Yahoo и т.д.)
4. Следуйте инструкциям для подключения вашего email аккаунта
5. После подключения скопируйте **Service ID** (например: `service_xxxxxxx`)

## Шаг 3: Создание Email Template

1. Перейдите в раздел **Email Templates**
2. Нажмите **Create New Template**
3. В открывшемся окне "Select Template" у вас есть два варианта:

### Вариант A: Использовать готовый шаблон (быстрее)

1. В левой панели выберите категорию **"One-Time Password"** (Одноразовый пароль)
2. Нажмите кнопку **"Create Template"** (синяя кнопка справа внизу)
3. В редакторе шаблона найдите переменную для кода (обычно `{{code}}` или `{{otp}}`)
4. Замените её на `{{verification_code}}` (чтобы соответствовать нашему коду)
5. Настройте текст письма по желанию
6. Сохраните шаблон

### Вариант B: Создать свой шаблон (больше контроля)

1. В окне "Select Template" нажмите **"Create Template"** (можно выбрать любой шаблон как основу или создать с нуля)
2. В редакторе шаблона используйте следующий HTML код:

**Subject:**
```
Код подтверждения регистрации
```

**Content (HTML):**
```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .container {
            background-color: #f9f9f9;
            border-radius: 10px;
            padding: 30px;
            text-align: center;
        }
        .code {
            font-size: 32px;
            font-weight: bold;
            color: #4285f4;
            letter-spacing: 5px;
            margin: 20px 0;
            padding: 15px;
            background-color: #fff;
            border-radius: 8px;
            border: 2px solid #4285f4;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Код подтверждения регистрации</h2>
        <p>Здравствуйте!</p>
        <p>Ваш код подтверждения для регистрации:</p>
        <div class="code">{{verification_code}}</div>
        <p>Код действителен в течение <strong>10 минут</strong>.</p>
        <p>Если вы не запрашивали этот код, проигнорируйте это письмо.</p>
        <div class="footer">
            <p>С уважением,<br>Команда Real Estate Auction</p>
        </div>
    </div>
</body>
</html>
```

4. В настройках шаблона используйте следующие переменные:
   - `{{to_email}}` - email получателя
   - `{{verification_code}}` - код подтверждения
   - `{{from_name}}` - имя отправителя
   - `{{message}}` - сообщение

5. **Важно:** В настройках шаблона (Settings) убедитесь, что:
   - **To Email** = `{{to_email}}`
   - **Subject** = "Код подтверждения регистрации" (или любой другой заголовок)

6. Сохраните шаблон и скопируйте **Template ID** (например: `template_xxxxxxx`)
   
   **Где найти Template ID:**
   - После сохранения шаблона, Template ID будет показан вверху страницы
   - Или в списке шаблонов (Email Templates) - ID указан рядом с названием

## Шаг 4: Получение Public Key

1. Перейдите в раздел **Account** → **General**
2. Найдите **Public Key** и скопируйте его (например: `xxxxxxxxxxxxx`)

## Шаг 5: Настройка переменных окружения

Создайте или обновите файл `.env.local` в корне проекта:

```env
# EmailJS настройки
VITE_EMAILJS_SERVICE_ID=service_xxxxxxx
VITE_EMAILJS_TEMPLATE_ID=template_xxxxxxx
VITE_EMAILJS_PUBLIC_KEY=xxxxxxxxxxxxx
```

Замените значения на ваши реальные ID и ключи.

## Шаг 6: Установка зависимостей

Пакет `@emailjs/browser` уже добавлен в `package.json`. Установите зависимости:

```bash
npm install
```

Если пакет не установился автоматически, установите его вручную:

```bash
npm install @emailjs/browser
```

## Шаг 7: Проверка работы

1. Запустите приложение: `npm run dev`
2. Попробуйте зарегистрироваться с реальным email адресом
3. Проверьте почту - код должен прийти в течение нескольких секунд

## Важные замечания

### Лимиты EmailJS (бесплатный план):
- 200 писем в месяц
- Максимум 2 email сервиса
- Максимум 5 шаблонов

### Для production:
- Рассмотрите использование платного плана EmailJS
- Или настройте собственный backend с SMTP сервером
- Для больших объемов используйте специализированные сервисы (SendGrid, Mailgun, AWS SES)

### Безопасность:
- **НЕ** коммитьте файл `.env.local` в Git
- Добавьте `.env.local` в `.gitignore`
- Public Key EmailJS безопасен для использования на клиенте

## Альтернативные варианты

Если EmailJS не подходит, можно использовать:

1. **Backend API** - создайте endpoint для отправки email через SMTP
2. **SendGrid** - профессиональный сервис для email
3. **Mailgun** - еще один популярный сервис
4. **AWS SES** - для больших объемов

## Устранение неполадок

### Email не приходит:
1. Проверьте правильность Service ID, Template ID и Public Key
2. Убедитесь, что email сервис подключен в EmailJS Dashboard
3. Проверьте спам папку
4. Посмотрите логи в консоли браузера
5. Проверьте лимиты в EmailJS Dashboard

### Ошибка "EmailJS is not defined":
- Убедитесь, что пакет установлен: `npm install @emailjs/browser`
- Перезапустите dev сервер после установки

### Ошибка отправки:
- Проверьте, что все переменные окружения установлены
- Убедитесь, что шаблон использует правильные переменные
- Проверьте настройки email сервиса в EmailJS

