# Финальное решение проблемы "Configure network"

## Проблема:
Railway не может настроить сеть, даже если домен настроен и build проходит успешно.

## Что было сделано:

### 1. Добавлен Health Check Endpoint
- `/health` - возвращает статус приложения и информацию о портах
- Railway может использовать это для проверки готовности приложения

### 2. Улучшено логирование
- Сервер логирует информацию о портах при запуске
- Добавлена обработка ошибок

### 3. Настроены порты
- Vite слушает на `PORT` (Railway установит автоматически)
- Сервер слушает на `SERVER_PORT` (3000)
- Оба слушают на `0.0.0.0`

## Критически важно:

### В Railway Dashboard → Settings → Variables:
**ОБЯЗАТЕЛЬНО установите:**
- `SERVER_PORT=3000`

### Проверка логов:
После деплоя в логах должны быть:
1. От SERVER:
   ```
   🚀 Сервер запущен на порту 3000
   📡 API доступен по адресу: http://0.0.0.0:3000/api
   🌐 Railway PORT: [номер порта]
   🔧 SERVER_PORT: 3000
   ```

2. От FRONTEND:
   ```
   VITE v5.x.x  ready in xxx ms
   ➜  Local:   http://0.0.0.0:[PORT]/
   ```

## Если проблема сохраняется:

### Вариант 1: Проверьте логи
1. Откройте Railway Dashboard → View logs
2. Найдите сообщения о запуске процессов
3. Если одного из процессов нет - проблема в запуске

### Вариант 2: Проверьте переменные окружения
Убедитесь, что `SERVER_PORT=3000` установлено в Variables

### Вариант 3: Проверьте Networking
1. Settings → Networking
2. Убедитесь, что сервис экспонирован
3. Должен быть публичный домен

### Вариант 4: Health Check
Попробуйте открыть в браузере:
- `https://your-domain.railway.app/health` - должен вернуть JSON со статусом

## Возможная причина:
Railway может пытаться подключиться к приложению до того, как оба процесса полностью запустятся. В этом случае нужно подождать немного или проверить, что процессы запускаются быстро.

## Следующие шаги:
1. Закоммитьте все изменения
2. Задеплойте снова
3. Проверьте логи - должны быть сообщения от обоих процессов
4. Если ошибка повторится - пришлите полные логи из Railway
