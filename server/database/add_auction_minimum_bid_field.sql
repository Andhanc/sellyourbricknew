-- Добавление поля auction_minimum_bid в таблицу properties
-- Это поле хранит минимальную сумму следующей ставки
-- Если поле уже существует, команда выдаст ошибку, но это нормально

-- Проверяем, существует ли поле, и добавляем его только если его нет
-- SQLite не поддерживает IF NOT EXISTS для ALTER TABLE, поэтому используем проверку через PRAGMA

