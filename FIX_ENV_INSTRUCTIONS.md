# Инструкция по исправлению .env файла для Clerk

## 🔴 Проблемы в текущем .env:

1. **`NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`** - это для Next.js, не для Vite. **УДАЛИТЬ!**
2. **`CLERK_SECRET_KEY`** - это серверный ключ, НЕ должен быть в клиентском коде. **УДАЛИТЬ!**
3. **`VITE_CLERK_PUBLISHABLE_KEY`** - ключ выглядит неполным или неправильным

## ✅ Правильная настройка:

В вашем `.env` файле должна быть **ТОЛЬКО ОДНА** строка для Clerk:

```
VITE_CLERK_PUBLISHABLE_KEY=pk_test_bWVldC1ob3VuZC01NC5jbGVyay5hY2NvdW50cy5kZXYk
```

## 📝 Что нужно сделать:

1. Откройте файл `.env` в редакторе
2. **УДАЛИТЕ** эти строки:
   - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=...` (строка 5)
   - `CLERK_SECRET_KEY=...` (строка 6)
3. **ЗАМЕНИТЕ** строку 8 (`VITE_CLERK_PUBLISHABLE_KEY`) на:
   ```
   VITE_CLERK_PUBLISHABLE_KEY=pk_test_bWVldC1ob3VuZC01NC5jbGVyay5hY2NvdW50cy5kZXYk
   ```
   (Используйте ключ из строки 5, но с префиксом `VITE_`)

## ⚠️ Важно:

- **`CLERK_SECRET_KEY`** (начинается с `sk_test_`) - это секретный ключ для сервера
- Он **НИКОГДА** не должен быть в клиентском коде
- Он используется только на бэкенде для серверных операций
- Если он попадет в клиентский код, это серьезная уязвимость безопасности!

## 🔍 Как проверить правильный ключ:

1. Откройте [Clerk Dashboard](https://dashboard.clerk.com/)
2. Перейдите в **API Keys** (https://dashboard.clerk.com/last-active?path=api-keys)
3. Найдите **Publishable Key** (должен начинаться с `pk_test_` или `pk_live_`)
4. Скопируйте его и используйте в `.env` с префиксом `VITE_`

## ✅ После исправления:

1. Сохраните файл `.env`
2. Перезапустите dev-сервер (`npm run dev`)
3. Попробуйте войти через Google/Facebook снова


